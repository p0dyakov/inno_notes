---
draft: true
title: "W1. Foundations of Probability"
author: "Ramil Nasibullin"
date: "September 08, 2026"
format: html
engine: knitr
---
<!-- QUARANTINE: block-fix loop failed, manual finish needed -->


#### **1. Theory**

##### **1.1 Random Experiments, Empirical Probability, and Frequency Stabilization**

**Probability theory** is the mathematical framework dedicated to analyzing random phenomena, quantifying uncertainty, and modeling non-deterministic systems. 

###### **Definitions and Core Concepts**

A **random experiment** is an observational or experimental procedure whose specific outcome cannot be predetermined with certainty, even when conducted under strictly controlled, identical conditions. In contrast to deterministic physical processes (where identical inputs yield identical outputs), a random experiment possesses inherent variability. 

*   *Discrete examples:* Tossing a coin (possible outcomes: Heads $H$, Tails $T$); rolling a standard six-sided die (outcomes: $1, 2, 3, 4, 5, 6$).
*   *Continuous examples:* Measuring the height of a randomly selected student; recording the millisecond timestamp of an atmospheric static electrical pulse captured by a radio receiver.

When an experiment is repeated $n$ times under identical conditions, each repetition is called a **trial**. Let $A$ denote an event of interest, and let $m$ (or $m(A)$) denote the frequency—the number of trials in which event $A$ occurs.

The **empirical probability** (also called the **relative frequency**) of an event $A$, denoted $P_n(A)$, is the observed ratio of favorable occurrences to the total number of trials:
$$P_n(A) = \frac{m}{n} = \frac{\text{Number of times } A \text{ occurred}}{\text{Total number of trials } n}$$

###### **Why It Matters: Frequency Stabilization**

In physical experiments, the theoretical probability $P(A)$ cannot be directly inspected; it is an intrinsic property of the system. The bridge between the physical world and axiomatic probability is the phenomenon of **frequency stabilization**: as the number of trials increases ($n \to \infty$), the empirical relative frequency $m/n$ ceases to fluctuate wildly and stabilizes around a deterministic numerical constant $p \in [0, 1]$. This limiting constant is identified as the theoretical probability of $A$:
$$\lim_{n \to \infty} \frac{m}{n} = P(A)$$

###### **Empirical Demonstration and Error Scaling**

Consider a balanced coin tossed $n$ times with $p = P(\text{Heads}) = 0.5$. Recording the number of heads $m$ across 10 distinct experimental runs at increasing scales ($n = 10^2, 10^4, 10^6$) reveals how fluctuations shrink:

| Run ($N$) | $n = 10^2$ ($m$) | $n = 10^4$ ($m$) | $n = 10^6$ ($m$) |
| :---: | :---: | :---: | :---: |
| 1 | 41 | 4985 | 499558 |
| 2 | 48 | 5004 | 499952 |
| 3 | 44 | 5085 | 500114 |
| 4 | 52 | 4946 | 500064 |
| 5 | 58 | 4978 | 500183 |
| 6 | 52 | 4985 | 499533 |
| 7 | 45 | 5012 | 500065 |
| 8 | 50 | 4931 | 500317 |
| 9 | 52 | 5016 | 500449 |
| 10 | 45 | 4973 | 500704 |
| **Typical Error ($|p_n - p|$)** | $\sim 10^{-1}$ | $\sim 10^{-2}$ | $\sim 10^{-3}$ |

At $n = 10^2$, the observed counts range from $41$ to $58$, yielding relative frequencies between $0.41$ and $0.58$ (an absolute error on the order of $10^{-1}$). At $n = 10^4$, frequencies cluster tightly between $0.4931$ and $0.5085$ (error $\sim 10^{-2}$). At $n = 10^6$, frequencies stay within $0.499533$ to $0.500704$ (error $\sim 10^{-3}$). The empirical error $|m/n - p|$ decays at the rate of $\mathcal{O}(1/\sqrt{n})$: gaining one additional decimal place of precision requires a hundredfold increase in sample size.

###### **Worked Mini-Illustration**

A network interface monitors incoming packets for checksum corruption. In an experiment consisting of $n = 1000$ transmitted packets, $m = 510$ packets arrive with the leading status bit set to high. The empirical probability of observing this bit state is:
$$P_{1000}(\text{High Bit}) = \frac{510}{1000} = 0.51$$
This value serves as an empirical estimate for the system's underlying parameter $p$.

###### **Limitations of Empirical Probability**

1.  **Requirement of Infinite Repeatability:** Empirical estimation presupposes that identical conditions can be replicated indefinitely. It cannot assign probabilities to unique, non-repeatable events (e.g., "What is the probability of a major server outage tomorrow?").
2.  **Incompleteness as a Formal Definition:** Because $n \to \infty$ is an idealized mathematical limit never achievable in physical reality, $m/n$ remains an estimate subject to statistical uncertainty, not an absolute identity.

###### **Common Pitfalls**

*   **Confusing Relative Frequency with True Probability:** The relative frequency $m/n$ is an empirical, sample-dependent random variable that changes with every run of $n$ trials. The true probability $P(A)$ is an invariant mathematical parameter.
*   **The Gambler's Fallacy (Law of Averages Misconception):** Frequency stabilization occurs by **dilution**, not **compensation**. If a fair coin lands on Tails 10 times in a row, the next toss is not "due" to be Heads. The probability of Heads on trial 11 remains strictly $0.5$. The initial streak of 10 Tails becomes negligible as $n$ grows to millions because the denominator $n$ expands, not because the coin remembers past outcomes.

##### **1.2 Sample Spaces and Event Classification**

A rigorous probabilistic model begins by formalizing the universe of all conceivable results of a random experiment.

The **sample space**, typically denoted by $S$ or $\Omega$, is the set of all possible outcomes of a random experiment. An individual result $\omega \in S$ is called an **outcome** (or **sample point**). The outcomes in $S$ must be mutually exclusive (no two distinct outcomes can occur simultaneously) and collectively exhaustive (every run of the experiment terminates in at least one outcome in $S$).

```
                      ┌──────────────────────────────────────────────┐
                      │             Sample Space S                   │
                      │                                              │
                      │   Certain Event: S                           │
                      │   ┌──────────────────────────────────────┐   │
                      │   │  Compound Event: A = {ω₁, ω₂}        │   │
                      │   │   ┌────────┐        ┌────────┐       │   │
                      │   │   │   ω₁   │        │   ω₂   │       │   │
                      │   │   └────────┘        └────────┘       │   │
                      │   │  Simple Event      Simple Event      │   │
                      │   │    E₁ = {ω₁}        E₂ = {ω₂}        │   │
                      │   └──────────────────────────────────────┘   │
                      │                                              │
                      │   Impossible (Null) Event: ∅ = { }           │
                      └──────────────────────────────────────────────┘
```

###### **1.2.1 Classification of Sample Spaces by Cardinality**

Sample spaces are categorized according to their mathematical cardinality $|S|$:

1. **Finite sample space:** $S$ contains a finite number of elements ($|S| < \infty$).
   * Tossing a single coin: $S = \{H, T\}$, where $|S| = 2$.
   * Rolling a standard six-sided die: $S = \{1, 2, 3, 4, 5, 6\}$, where $|S| = 6$.
   * Tossing a coin three consecutive times: $S = \{HHH, HHT, HTH, THH, HTT, THT, TTH, TTT\}$, where $|S| = 2^3 = 8$.
2. **Countably infinite sample space:** $S$ has an infinite number of outcomes that can be put into a one-to-one correspondence with the set of natural numbers $\mathbb{N}_0 = \{0, 1, 2, \dots\}$.
   * Counting the number of email arrivals at a server over the span of an hour: $S = \{0, 1, 2, \dots\}$.
   * Counting the number of tosses of a coin required until the first Head appears: $S = \{1, 2, 3, \dots\}$.
3. **Uncountably infinite (continuous) sample space:** $S$ consists of a continuum of real values (such as an interval or a region in Euclidean space $\mathbb{R}^n$).
   * Measuring the operational lifetime $t$ (in years) of an electronic component until failure: $S = [0, \infty) = \{t \in \mathbb{R} \mid t \ge 0\}$.
   * Measuring the electrical voltage across a resistor: $S = \mathbb{R}$.
   * Recording ambient temperature measurements: $S = \mathbb{R}$ (or $S = [0, \infty)$ on the absolute Kelvin scale).
   * Locating the exact spatial position of a particle inside a physical chamber: $S = \mathbb{R}^3$.

###### **1.2.2 Granularity in Sample Space Modeling**

A physical experiment does not possess a single, uniquely determined sample space. The choice of $S$ depends directly on the inquiry being modeled and the required level of granularity:

* Consider tossing a standard six-sided die. If the observer records the number appearing on the top face, the sample space is:
  $$S_1 = \{1, 2, 3, 4, 5, 6\}$$
* If the observer only cares whether the resulting number is even or odd, an equally valid sample space is:
  $$S_2 = \{\text{even}, \text{odd}\}$$

$S_1$ is a finer-grained representation than $S_2$. A finer sample space contains strictly more information: any event defined on $S_2$ can be recovered as a union of outcomes in $S_1$ (e.g., $\text{even} \equiv \{2, 4, 6\}$), but the converse is not true. Choosing an appropriate sample space balances mathematical tractability with sufficient descriptive power.

###### **1.2.3 Event Classification**

An **event** $A$ is a subset of the sample space, denoted $A \subseteq S$. An event $A$ is said to **occur** in a trial of the experiment if and only if the realized outcome $\omega$ satisfies $\omega \in A$. Events are classified based on their structure and certainty:

* **Simple event (elementary event):** An event containing exactly one outcome, formalized as a singleton set $\{\omega\} \subseteq S$.
  * In a single coin toss, observing Heads corresponds to the simple event $\{H\}$.
* **Compound event:** An event containing two or more distinct outcomes.
  * In rolling a six-sided die with $S = \{1, 2, 3, 4, 5, 6\}$, the event "rolling an even number" is the compound subset $A = \{2, 4, 6\}$.
  * The event "rolling a number strictly greater than 4" is the compound subset $B = \{5, 6\}$.
* **Certain event:** The entire sample space $S$ viewed as an event ($A = S$). Because every trial of the experiment must produce some outcome $\omega \in S$, the condition $\omega \in S$ is tautologically satisfied. The certain event occurs on every trial.
  * In a single coin toss, the event "getting either Heads or Tails" is $\{H, T\} = S$.
* **Impossible event (null event):** The empty set $\emptyset \subseteq S$, which contains no outcomes. Because no possible outcome $\omega$ satisfies $\omega \in \emptyset$, this event cannot occur in any trial.
  * In a biological trial, the event of detecting a microscopic bacterium using the unaided human eye is $A = \emptyset$.
  * In a die-rolling trial with outcomes in $\{1, 2, 3, 4, 5, 6\}$, the event $B = \{x \mid x \text{ is an even factor of } 7\}$ is the null event $\emptyset$, since the only factors of $7$ are the odd integers $1$ and $7$.

###### **1.2.4 Why Event Classification Matters**

Classifying outcomes into subsets creates the bridge between raw experimental phenomena and set-theoretic mathematics. In discrete spaces, specifying the behavior of simple events often completely characterizes the model. In continuous spaces, individual outcomes $\{\omega\}$ carry zero individual weight, making compound intervals and region subsets the primary objects of probabilistic study.

###### **1.2.5 Worked Mini-Illustration**

Consider a quality-assurance test measuring the operational lifespan $t$ (in years) of a semiconductor component. 

* The sample space is continuous: $S = \{t \in \mathbb{R} \mid t \ge 0\} = [0, \infty)$.
* The event $A$ that the component fails before the end of its fifth year is the subset:
  $$A = \{t \in S \mid 0 \le t < 5\} = [0, 5)$$
* The event $B$ that the component operates for at least 10 years without failure is:
  $$B = \{t \in S \mid t \ge 10\} = [10, \infty)$$
* If a component fails at exactly $t = 3.2$ years, the outcome is $\omega = 3.2$. Because $3.2 \in [0, 5)$, event $A$ occurred. Because $3.2 \notin [10, \infty)$, event $B$ did not occur.
* The event $C = \{t \in S \mid t < 0\}$ is the impossible event $\emptyset$, as lifetime cannot be negative.
* The event $D = \{t \in S \mid t \ge 0\}$ is the certain event $S$.

###### **1.2.6 Common Pitfalls**

* **Confusing an outcome with a simple event:** An outcome $\omega$ is an *element* of $S$ ($\omega \in S$), whereas a simple event is a *subset* containing that element ($\{\omega\} \subseteq S$). Probability measures act on subsets (events), not on bare elements: one writes $P(\{\omega\})$, not $P(\omega)$.
* **Assuming $S$ is fixed for a real-world system:** Believing an experiment has only one "correct" sample space causes errors when translating verbal specifications into sets. As demonstrated by $S_1 = \{1, 2, 3, 4, 5, 6\}$ versus $S_2 = \{\text{even}, \text{odd}\}$, the sample space must be chosen with sufficient detail to answer the specific research question.
* **Treating continuous outcomes as compound events:** In continuous spaces such as $S = [0, \infty)$, a singleton $\{t_0\}$ is still technically a simple event, even though its probability under continuous distributions is $0$. It must not be confused with a positive-length interval $[t_1, t_2]$, which is a compound event containing uncountably many outcomes.

##### **1.3 Set Operations on Events and Algebraic Identities**

Probability evaluates uncertainty by mapping real-world logical propositions onto the algebra of subsets within a sample space $S$. Every compound event formed by connective words such as *not*, *and*, *or*, and *exclusive or* corresponds directly to a set-theoretic operation.

###### **1.3.1 Fundamental Event Operations**

Let $S$ be the sample space, and let $A, B \subseteq S$ be events.

*   **Complement:** The **complement** of an event $A$, denoted $A^c$ (or $A'$), is the set of all outcomes in $S$ that do not belong to $A$:
    $$A^c = \{\omega \in S : \omega \notin A\}$$
    Probabilistically, $A^c$ represents the event that *event $A$ does not occur*.
*   **Intersection:** The **intersection** of two events $A$ and $B$, denoted $A \cap B$, is the set of all outcomes common to both $A$ and $B$:
    $$A \cap B = \{\omega \in S : \omega \in A \text{ and } \omega \in B\}$$
    Probabilistically, $A \cap B$ represents the event that *both events $A$ and $B$ occur simultaneously*.
*   **Union:** The **union** of two events $A$ and $B$, denoted $A \cup B$, is the set of all outcomes belonging to $A$, to $B$, or to both:
    $$A \cup B = \{\omega \in S : \omega \in A \text{ or } \omega \in B\}$$
    Probabilistically, $A \cup B$ represents the event that *at least one of the events $A$ or $B$ occurs*.
*   **Set Difference:** The **difference** of event $B$ from event $A$, denoted $A \setminus B$, contains outcomes that belong to $A$ but not to $B$:
    $$A \setminus B = \{\omega \in S : \omega \in A \text{ and } \omega \notin B\} = A \cap B^c$$
    Probabilistically, $A \setminus B$ represents the event that *event $A$ occurs while event $B$ does not occur*.
*   **Symmetric Difference:** The **symmetric difference** of events $A$ and $B$, denoted $A \Delta B$, is the set of outcomes belonging to either $A$ or $B$, but not to both:
    $$A \Delta B = (A \setminus B) \cup (B \setminus A) = (A \cup B) \setminus (A \cap B)$$
    Probabilistically, $A \Delta B$ represents the event that *exactly one of the two events occurs*.
*   **Mutual Exclusivity (Disjointness):** Two events $A$ and $B$ are **mutually exclusive** (or **disjoint**) if they share no sample points:
    $$A \cap B = \emptyset$$
    Mutually exclusive events cannot occur simultaneously in a single trial of an experiment. When a collection of pairwise disjoint events forms a union, it is often denoted by the disjoint union symbol $\bigsqcup$ or $\sum$.

###### **1.3.2 Fundamental Set Identities**

Operations on events obey the standard axioms of Boolean algebra. For any event $A \subseteq S$ and empty set $\emptyset$:

1.  **Empty Set Identities:** $A \cap \emptyset = \emptyset$, and $A \cup \emptyset = A$.
2.  **Complement Boundaries:** $A \cap A^c = \emptyset$, and $A \cup A^c = S$.
3.  **Extremal Complements:** $S^c = \emptyset$, and $\emptyset^c = S$.
4.  **Involution (Double Complement):** $(A^c)^c = A$.
5.  **Disjoint Partition Decomposition:** Any event $A$ decomposes into disjoint sub-events relative to any other event $B$:
    $$A = (A \cap B) \sqcup (A \setminus B) = (A \cap B) \sqcup (A \cap B^c)$$

###### **1.3.3 De Morgan's Laws**

**De Morgan's laws** state that the complement of an intersection is the union of the complements, and the complement of a union is the intersection of the complements:

$$(A \cap B)^c = A^c \cup B^c \quad \text{and} \quad (A \cup B)^c = A^c \cap B^c$$

For an arbitrary collection of $n$ events $A_1, A_2, \dots, A_n$, the laws generalize to:

$$\left(\bigcap_{i=1}^n A_i\right)^c = \bigcup_{i=1}^n A_i^c \quad \text{and} \quad \left(\bigcup_{i=1}^n A_i\right)^c = \bigcap_{i=1}^n A_i^c$$

###### **Element-wise Proof of De Morgan's Law for Three Events**

To verify $(A_1 \cap A_2 \cap A_3)^c = A_1^c \cup A_2^c \cup A_3^c$, show that an outcome $\omega \in S$ belongs to the left-hand side if and only if it belongs to the right-hand side via bidirectional logical equivalences:

$$\begin{aligned}
\omega \in (A_1 \cap A_2 \cap A_3)^c 
&\iff \omega \notin (A_1 \cap A_2 \cap A_3) \\
&\iff \neg (\omega \in A_1 \land \omega \in A_2 \land \omega \in A_3) \\
&\iff (\omega \notin A_1) \lor (\omega \notin A_2) \lor (\omega \notin A_3) \\
&\iff (\omega \in A_1^c) \lor (\omega \in A_2^c) \lor (\omega \in A_3^c) \\
&\iff \omega \in (A_1^c \cup A_2^c \cup A_3^c)
\end{aligned}$$

Because the membership condition is identical on both sides, the sets are equal. In probabilistic terms: the negation of "all three events occur" is logically equivalent to "at least one of the three events does not occur."

###### **1.3.4 Worked Illustration: System Availability**

Consider a computing cluster composed of three independent nodes. Let $A_i$ represent the event that "server $i$ is operational" ($i \in \{1, 2, 3\}$). The logical statements describing the cluster state translate into algebraic set expressions:

*   **All three servers are operational:**
    $$D_1 = A_1 \cap A_2 \cap A_3$$
*   **At least one server is down:** This event is the exact complement of $D_1$. By De Morgan's law:
    $$D_2 = D_1^c = (A_1 \cap A_2 \cap A_3)^c = A_1^c \cup A_2^c \cup A_3^c$$
*   **Exactly one server is operational:** Partition into three pairwise disjoint cases where one server runs and the remaining two fail:
    $$D_3 = (A_1 \cap A_2^c \cap A_3^c) \cup (A_1^c \cap A_2 \cap A_3^c) \cup (A_1^c \cap A_2^c \cap A_3)$$
*   **At least two servers are operational:** This condition requires that either two servers run or all three run. Taking the pairwise intersections accounts for all required configurations:
    $$D_4 = (A_1 \cap A_2) \cup (A_1 \cap A_3) \cup (A_2 \cap A_3)$$
    *Note:* An explicit fourth term for all three servers running ($A_1 \cap A_2 \cap A_3$) is redundant because $(A_1 \cap A_2 \cap A_3) \subset (A_1 \cap A_2)$.

Now let individual outcomes be written as ternary binary strings $\omega = x_1x_2x_3 \in \{0, 1\}^3$, where $1$ denotes up and $0$ denotes down. The sample space is $S = \{111, 110, 101, 011, 100, 010, 001, 000\}$. Evaluating the formulas on $S$:

*   $D_1 = \{111\}$
*   $D_2 = S \setminus \{111\} = \{110, 101, 011, 100, 010, 001, 000\}$
*   $D_3 = \{100, 010, 001\}$
*   $D_4 = \{110, 101, 011, 111\}$

###### **1.3.5 Common Pitfalls**

*   **Inclusive Union vs. Exclusive Union:** In mathematics, $A \cup B$ is an *inclusive OR* (outcomes in $A$, in $B$, or in both). Natural language frequently uses "or" in an *exclusive* sense ("you can have coffee or tea"). To express the exclusive occurrence of either $A$ or $B$, the symmetric difference $A \Delta B = (A \cup B) \setminus (A \cap B)$ must be used rather than the union $A \cup B$.
*   **De Morgan Operator Swapping:** A common error is distributing a complement over an expression without toggling the operator, writing $(A \cap B)^c = A^c \cap B^c$ instead of $A^c \cup B^c$. The set $A^c \cap B^c$ represents "neither event occurs", which is the complement of the union $(A \cup B)^c$.
*   **Mutually Exclusive vs. Independent:** Mutually exclusive events satisfy $A \cap B = \emptyset$, meaning that the occurrence of one strictly precludes the occurrence of the other. Independence is an entirely different multiplicative property concerning probability values ($P(A \cap B) = P(A)P(B)$). If two non-trivial events ($P(A) > 0, P(B) > 0$) are mutually exclusive, they cannot be independent, because $P(A \cap B) = P(\emptyset) = 0 \neq P(A)P(B)$.

##### **1.4 σ-Algebras and Measurable Spaces**

In discrete probability spaces, one often assumes that any arbitrary subset of the sample space $S$ constitutes a valid event that can be assigned a probability. However, when transitioning to uncountably infinite sample spaces such as $S = \mathbb{R}$ or $S = [0, 1]$, this naive assumption collapses: measure theory demonstrates that it is mathematically impossible to assign a geometrically consistent, countably additive probability measure to every subset of the power set $\mathcal{P}(S)$ (a consequence linked to the existence of non-measurable sets, such as Vitali sets and the Banach-Tarski paradox under the Axiom of Choice). Consequently, probability theory restricts the domain of valid events to a well-behaved family of subsets known as a **$\sigma$-algebra**.

###### **1.4.1 Formal Definition and Measurable Spaces**

Let $S$ be a non-empty sample space. A collection $\mathcal{F}$ of subsets of $S$ is called a **$\sigma$-algebra** (or **$\sigma$-field**) on $S$ if it satisfies the following three axioms:

1. **Containment of the whole space:**
   $$S \in \mathcal{F}$$
2. **Closure under complementation:**
   $$\text{If } A \in \mathcal{F}, \text{ then } A^c \in \mathcal{F} \quad (\text{where } A^c = S \setminus A)$$
3. **Closure under countable unions:**
   $$\text{If } A_1, A_2, A_3, \dots \in \mathcal{F}, \text{ then } \bigcup_{i=1}^{\infty} A_i \in \mathcal{F}$$

The pair $(S, \mathcal{F})$ is formally designated as a **measurable space**, and any set $A \in \mathcal{F}$ is called an **$\mathcal{F}$-measurable set** or simply an **event**.

From these axioms, two immediate properties follow:

* **Containment of the empty set:** Since $S \in \mathcal{F}$, Axiom 2 guarantees that $\emptyset = S^c \in \mathcal{F}$.
* **Closure under countable intersections:** Applying De Morgan's laws, if $A_1, A_2, \dots \in \mathcal{F}$, then each $A_i^c \in \mathcal{F}$, which implies
  $$\bigcap_{i=1}^\infty A_i = \left(\bigcup_{i=1}^\infty A_i^c\right)^c \in \mathcal{F}$$
* **Finite operations:** Any finite sequence $A_1, \dots, A_n \in \mathcal{F}$ can be extended to an infinite sequence by defining $A_k = \emptyset$ for all $k > n$. Thus, a $\sigma$-algebra is automatically closed under finite unions and finite intersections. On a finite sample space $S$, the requirement of countable unions reduces entirely to closure under finite unions.

###### **1.4.2 Catalog of Canonical σ-Algebras**

Given a non-empty set $S$, several standard $\sigma$-algebras arise:

1. **Trivial (Minimal) $\sigma$-Algebra:**
   $$\mathcal{F}_{\text{trivial}} = \{\emptyset, S\}$$
   This represents the absolute minimum information: one only knows whether an outcome occurred in $S$ or not at all.
2. **Power Set (Maximal) $\sigma$-Algebra:**
   $$\mathcal{F}_{\max} = 2^S = \mathcal{P}(S)$$
   Contains every conceivable subset of $S$. If $S$ is finite with $|S| = k$, $|\mathcal{F}_{\max}| = 2^k$.
3. **Single-Subset $\sigma$-Algebra:**
   For any proper non-empty subset $A \subset S$, the smallest $\sigma$-algebra containing $A$ is:
   $$\mathcal{F}_A = \{\emptyset, A, A^c, S\}$$
4. **Countable-Cocountable $\sigma$-Algebra:**
   On an uncountable set $S$, the family of subsets that are either countable or have a countable complement:
   $$\mathcal{F}_{\text{count}} = \{A \subseteq S : A \text{ is countable or } A^c \text{ is countable}\}$$
   satisfies all three axioms.
5. **Borel $\sigma$-Algebra on $\mathbb{R}$:**
   The **Borel $\sigma$-algebra**, denoted $\mathcal{B}(\mathbb{R})$, is the $\sigma$-algebra generated by all open intervals on the real line:
   $$\mathcal{B}(\mathbb{R}) = \sigma(\{(a, b) \subset \mathbb{R} : -\infty < a < b < \infty\})$$
   It contains all open sets, closed sets, half-open intervals $[a, b)$, points $\{x\}$, and countable unions/intersections thereof, forming the rigorous foundation for continuous probability distributions without encountering non-measurable subsets.

###### **1.4.3 Generated σ-Algebras and Partition Atoms**

If $\mathcal{C}$ is an arbitrary collection of subsets of $S$, the **generated $\sigma$-algebra** $\sigma(\mathcal{C})$ is defined as the smallest $\sigma$-algebra on $S$ containing $\mathcal{C}$ (formally, the intersection of all $\sigma$-algebras on $S$ that contain $\mathcal{C}$).

When a $\sigma$-algebra is generated by a finite **partition** of the space $S$—that is, a collection of non-empty, pairwise disjoint sets $\{B_1, B_2, \dots, B_n\}$ such that $\bigcup_{i=1}^n B_i = S$—the sets $B_i$ are called the **atoms** (the indivisible elementary building blocks) of the $\sigma$-algebra. Every element in $\sigma(B_1, \dots, B_n)$ is formed by taking a union of a sub-collection of these atoms.

::: {.callout-note appearance="minimal"}
**Theorem (Size of a Partition-Generated $\sigma$-Algebra):**  
If a finite partition of $S$ consists of $n$ disjoint non-empty atoms, the $\sigma$-algebra generated by this partition contains exactly $2^n$ distinct sets.
:::

**Worked Example (Atoms and Generated Systems):**  
Let $X = \{a, b, c, d\}$. We wish to construct the generated $\sigma$-algebra $\sigma(\{a\}, \{b\})$.  
The base subsets are $\{a\}$ and $\{b\}$. To form a partition of $X$, determine the disjoint regions:
1. Atom 1: $\{a\}$
2. Atom 2: $\{b\}$
3. Atom 3: $(\{a\} \cup \{b\})^c = \{c, d\}$

These $n = 3$ atoms partition $X$. The generated $\sigma$-algebra consists of all $2^3 = 8$ possible unions of these three atoms:
$$\sigma(\{a\}, \{b\}) = \{\emptyset, \{a\}, \{b\}, \{c, d\}, \{a, b\}, \{a, c, d\}, \{b, c, d\}, X\}$$
Every complement is present (e.g., $\{a\}^c = \{b, c, d\}$, $\{c, d\}^c = \{a, b\}$), and unions of atoms remain within the system.

###### **1.4.4 Set-Theoretic Closure: Intersections vs. Unions of σ-Algebras**

A recurring question in probability theory is whether combining two valid event spaces $\mathcal{F}_1$ and $\mathcal{F}_2$ on the same sample space $S$ yields another valid event space.

::: {.callout-note appearance="minimal"}
**Theorem (Intersection of $\sigma$-Algebras):**  
Let $\mathcal{F}_1$ and $\mathcal{F}_2$ be two $\sigma$-algebras on $S$. Then their intersection $\mathcal{F}_1 \cap \mathcal{F}_2$ is also a $\sigma$-algebra on $S$.
:::

*Proof:*
1. **Whole space:** Since $\mathcal{F}_1$ and $\mathcal{F}_2$ are $\sigma$-algebras, $S \in \mathcal{F}_1$ and $S \in \mathcal{F}_2$. Thus, $S \in \mathcal{F}_1 \cap \mathcal{F}_2$.
2. **Complements:** Let $A \in \mathcal{F}_1 \cap \mathcal{F}_2$. Then $A \in \mathcal{F}_1 \implies A^c \in \mathcal{F}_1$, and $A \in \mathcal{F}_2 \implies A^c \in \mathcal{F}_2$. Therefore, $A^c \in \mathcal{F}_1 \cap \mathcal{F}_2$.
3. **Countable unions:** Let $A_1, A_2, \dots \in \mathcal{F}_1 \cap \mathcal{F}_2$. Because each $A_i \in \mathcal{F}_1$, $\bigcup_{i=1}^\infty A_i \in \mathcal{F}_1$. Similarly, because each $A_i \in \mathcal{F}_2$, $\bigcup_{i=1}^\infty A_i \in \mathcal{F}_2$. Hence, $\bigcup_{i=1}^\infty A_i \in \mathcal{F}_1 \cap \mathcal{F}_2$.  
All three conditions are satisfied; $\mathcal{F}_1 \cap \mathcal{F}_2$ is a $\sigma$-algebra. $\blacksquare$

::: {.callout-caution appearance="minimal"}
**Pitfall (Union of $\sigma$-Algebras is NOT Generally a $\sigma$-Algebra):**  
The set-theoretic union $\mathcal{F}_1 \cup \mathcal{F}_2$ generally fails the closure under unions requirement.
:::

**Counterexample:**  
Let $S = \{1, 2, 3\}$. Consider the two valid single-subset $\sigma$-algebras:
$$\mathcal{F}_1 = \{\emptyset, \{1\}, \{2, 3\}, S\}, \qquad \mathcal{F}_2 = \{\emptyset, \{2\}, \{1, 3\}, S\}$$
Their union is:
$$\mathcal{F}_1 \cup \mathcal{F}_2 = \{\emptyset, \{1\}, \{2\}, \{2, 3\}, \{1, 3\}, S\}$$
While this collection contains $\emptyset$ and $S$, and is closed under complements ($\{1\}^c = \{2, 3\}$, $\{2\}^c = \{1, 3\}$), consider the union of the two elements $\{1\} \in \mathcal{F}_1 \cup \mathcal{F}_2$ and $\{2\} \in \mathcal{F}_1 \cup \mathcal{F}_2$:
$$\{1\} \cup \{2\} = \{1, 2\}$$
However, $\{1, 2\} \notin \mathcal{F}_1 \cup \mathcal{F}_2$. Thus, closure under unions fails, proving that $\mathcal{F}_1 \cup \mathcal{F}_2$ is not a $\sigma$-algebra. To combine the information of two $\sigma$-algebras, one must construct the generated $\sigma$-algebra $\sigma(\mathcal{F}_1 \cup \mathcal{F}_2)$, which in this example corresponds to the full power set $2^S$ of 8 elements.

##### **1.5 The Kolmogorov Axioms of Probability**

A **probability measure** $\mathbb{P}$ on a measurable space $(S, \mathcal{F})$ is a real-valued function $\mathbb{P} : \mathcal{F} \to [0, 1]$ (or $\mathbb{P} : \mathcal{F} \to \mathbb{R}$) that assigns a real number to each event in the $\sigma$-algebra $\mathcal{F}$ while satisfying the three **Kolmogorov axioms**:

1. **Axiom 1 (Non-negativity):** For every event $A \in \mathcal{F}$,
   $$\mathbb{P}(A) \ge 0.$$
2. **Axiom 2 (Normalization):** For the certain event (the entire sample space $S$),
   $$\mathbb{P}(S) = 1.$$
3. **Axiom 3 (Countable Additivity / $\sigma$-Additivity):** For any countable sequence of pairwise disjoint (mutually exclusive) events $A_1, A_2, A_3, \dots \in \mathcal{F}$ such that $A_i \cap A_j = \emptyset$ for all $i \ne j$,
   $$\mathbb{P}\left(\bigcup_{i=1}^{\infty} A_i\right) = \sum_{i=1}^{\infty} \mathbb{P}(A_i).$$

The ordered triple $(S, \mathcal{F}, \mathbb{P})$ is called a **probability space**.

```
  +-------------------------------------------------------------+
  | Probability Space Triple: (S, F, P)                         |
  |                                                             |
  |   S : Sample Space      (set of all possible outcomes)      |
  |   F : sigma-Algebra     (collection of measurable events)   |
  |   P : Measure Function  (satisfies Axioms 1, 2, and 3)      |
  +-------------------------------------------------------------+
```

###### **Why Kolmogorov's Axiomatization Matters**

Before Andrei Kolmogorov published *Foundations of the Theory of Probability* in 1933, probability relied on intuitive, ad-hoc notions such as empirical frequencies or classical equal-likelihood counting. These informal frameworks broke down when dealing with continuous variables, infinite sequences of trials, or geometric paradoxes. By embedding probability into rigorous measure theory, Kolmogorov established a minimal set of non-contradictory rules from which the entire calculus of probability—including the laws of large numbers and the central limit theorem—can be logically derived.

###### **Verifying Probability Measures on Discrete Spaces**

When $S$ is finite or countably infinite and $\mathcal{F} = \mathcal{P}(S)$, any assignment of weights $w_k = \mathbb{P}(\{\omega_k\})$ to elementary outcomes $\omega_k \in S$ defines a valid probability measure if and only if two conditions hold:
1. Every weight is non-negative: $w_k \ge 0$ for all $k$.
2. The total mass equals unity: $\sum_k w_k = 1$.

For any event $A \subseteq S$, its probability is defined unambiguously by countable additivity as:
$$\mathbb{P}(A) = \sum_{\omega_k \in A} w_k.$$

###### **Worked Mini-Illustration: Countable Sample Space**

Consider the countably infinite sample space $S = \{1, 2, 3, \dots\}$. Suppose each singleton outcome has weight assignment:
$$\mathbb{P}(\{k\}) = \frac{c}{3^k}, \quad k \in \{1, 2, 3, \dots\}.$$

To find the constant $c$ that makes $\mathbb{P}$ a valid probability measure, apply Axiom 2 (Normalization) and Axiom 3 (Countable Additivity):
$$\mathbb{P}(S) = \sum_{k=1}^{\infty} \mathbb{P}(\{k\}) = \sum_{k=1}^{\infty} \frac{c}{3^k} = c \sum_{k=1}^{\infty} \left(\frac{1}{3}\right)^k = c \cdot \frac{\frac{1}{3}}{1 - \frac{1}{3}} = c \cdot \frac{1/3}{2/3} = \frac{c}{2}.$$

Setting $\mathbb{P}(S) = 1$ yields:
$$\frac{c}{2} = 1 \implies c = 2.$$

Since $w_k = 2 / 3^k > 0$ for all $k \ge 1$, Axiom 1 holds automatically. We can now evaluate specific events:

* **Probability of an even outcome:** Let $E = \{2, 4, 6, \dots\}$. By countable additivity:
  $$\mathbb{P}(E) = \sum_{m=1}^{\infty} \frac{2}{3^{2m}} = 2 \sum_{m=1}^{\infty} \left(\frac{1}{9}\right)^m = 2 \cdot \frac{\frac{1}{9}}{1 - \frac{1}{9}} = 2 \cdot \frac{1}{8} = \frac{1}{4}.$$
* **Probability of $k \ge 3$:** Summing over the tail:
  $$\mathbb{P}(\{k \in S : k \ge 3\}) = 1 - \mathbb{P}(\{1\}) - \mathbb{P}(\{2\}) = 1 - \frac{2}{3} - \frac{2}{9} = \frac{9 - 6 - 2}{9} = \frac{1}{9}.$$

###### **Non-Examples: Testing Candidate Functions on Finite Spaces**

Let $S = \{1, 2, 3, 4\}$ with $\mathcal{F} = \mathcal{P}(S)$. Candidate functions may fail the axioms in characteristic ways:

1. **Failure of Normalization (Axiom 2):**  
   Suppose $\mathbb{P}(\{1\}) = \mathbb{P}(\{2\}) = \mathbb{P}(\{3\}) = 0.3$ and $\mathbb{P}(\{4\}) = 0.2$. Non-negativity holds ($w_k \ge 0$), but:
   $$\mathbb{P}(S) = 0.3 + 0.3 + 0.3 + 0.2 = 1.1 \ne 1.$$
   The total probability mass exceeds 1, so $\mathbb{P}$ is not a probability measure.
2. **Failure of Non-negativity (Axiom 1):**  
   Suppose $\mathbb{P}(\{1\}) = 0.5, \mathbb{P}(\{2\}) = 0.3, \mathbb{P}(\{3\}) = 0.4$, and $\mathbb{P}(\{4\}) = -0.2$. The sum of weights is:
   $$0.5 + 0.3 + 0.4 - 0.2 = 1.0.$$
   Normalization holds, but $\mathbb{P}(\{4\}) = -0.2 < 0$, violating Axiom 1.
3. **Failure of Additivity (Axiom 3) via Non-linear Set Functions:**  
   Define $\mathbb{P}(A) = \left(\frac{|A|}{4}\right)^2$ for every $A \subseteq S$.  
   * Non-negativity holds: $\mathbb{P}(A) \ge 0$ for all $A$.  
   * Normalization holds: $\mathbb{P}(S) = (4/4)^2 = 1$.  
   * Test additivity on disjoint sets $A = \{1\}$ and $B = \{2\}$:
     $$\mathbb{P}(\{1\}) = \left(\frac{1}{4}\right)^2 = \frac{1}{16}, \quad \mathbb{P}(\{2\}) = \left(\frac{1}{4}\right)^2 = \frac{1}{16},$$
     $$\mathbb{P}(\{1\}) + \mathbb{P}(\{2\}) = \frac{1}{16} + \frac{1}{16} = \frac{1}{8}.$$
     However, for their union $A \cup B = \{1, 2\}$:
     $$\mathbb{P}(\{1, 2\}) = \left(\frac{2}{4}\right)^2 = \frac{4}{16} = \frac{1}{4}.$$
     Since $\frac{1}{8} \ne \frac{1}{4}$, additivity fails. Squaring (or applying any non-linear transformation to) a proportional size measure destroys additivity.

###### **Pathology on Infinite Sets: The Finite/Infinite Set Function**

On the countably infinite set $S = \{1, 2, 3, \dots\}$, define the candidate function $Q : \mathcal{P}(S) \to [0, 1]$:
$$Q(A) = \begin{cases} 0, & \text{if } A \text{ is finite}, \\ 1, & \text{if } A \text{ is infinite}. \end{cases}$$

Checking the axioms reveals severe structural failures:

* **Axiom 1 (Non-negativity):** $Q(A) \in \{0, 1\} \ge 0$ holds.
* **Axiom 2 (Normalization):** $S$ is an infinite set, so $Q(S) = 1$ holds.
* **Axiom 3 (Additivity Failure):**
  * *Failure of finite additivity:* Partition $S$ into odd and even integers: $A = \{1, 3, 5, \dots\}$ and $B = \{2, 4, 6, \dots\}$. Clearly $A \cap B = \emptyset$ and $A \cup B = S$. Since both sets are infinite:
    $$Q(A) + Q(B) = 1 + 1 = 2 \quad \text{versus} \quad Q(A \cup B) = Q(S) = 1.$$
    Because $2 \ne 1$, finite additivity fails completely.
  * *Failure of countable additivity:* Express $S$ as the countable union of singletons: $S = \bigcup_{k=1}^{\infty} \{k\}$. Each singleton $\{k\}$ is finite, so $Q(\{k\}) = 0$. Summing the series yields:
    $$\sum_{k=1}^{\infty} Q(\{k\}) = \sum_{k=1}^{\infty} 0 = 0 \quad \text{versus} \quad Q(S) = 1.$$
    Because $0 \ne 1$, countable additivity fails. Hence $Q$ cannot serve as a probability measure.

###### **Common Pitfalls**

* **Applying Additivity to Overlapping Sets:** The formula $\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B)$ is valid **only** if $A \cap B = \emptyset$. If events overlap, adding their raw probabilities double-counts outcomes in $A \cap B$.
* **Confusing Finite Additivity with Countable Additivity:** Countable additivity ($\sigma$-additivity) is strictly stronger than finite additivity. A set function can satisfy $\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B)$ for all disjoint pairs without satisfying $\mathbb{P}\left(\bigcup_{i=1}^{\infty} A_i\right) = \sum_{i=1}^{\infty} \mathbb{P}(A_i)$ for infinite sequences. Axiom 3 explicitly requires countable additivity to allow taking limits and analyzing infinite random processes.
* **Assigning Probabilities to Outcomes Rather Than Sets:** A probability measure $\mathbb{P}$ is defined on the collection of subsets $\mathcal{F}$, not on the points $\omega \in S$ directly. Writing $\mathbb{P}(\omega)$ is mathematically informal; the rigorous notation is $\mathbb{P}(\{\omega\})$, the measure of the singleton event containing $\omega$.

##### **1.6 Fundamental Theorems and Properties Derived from the Axioms**

The Kolmogorov axioms specify only the minimal requirements for a measure on a measurable space $(S, \mathcal{F})$. Every operational theorem used in probability calculations—such as probabilities of impossible events, complements, subset relationships, and general bounds—is derived deductively from these three axioms.

###### **1.6.1 Probability of the Empty Set and Finite Additivity**

* **Probability of the Impossible Event:** The probability of the empty set $\emptyset$ is zero:
  $$\mathbb{P}(\emptyset) = 0$$
  * **Proof:** The sample space $S$ can be decomposed into the union of two disjoint sets: $S = S \cup \emptyset$, where $S \cap \emptyset = \emptyset$. Setting $A_1 = S$, $A_2 = \emptyset$, and $A_k = \emptyset$ for all $k \ge 3$, the sequence $\{A_k\}_{k=1}^\infty$ is pairwise disjoint. By countable additivity (Axiom 3) and normalization (Axiom 2):
    $$\mathbb{P}(S) = \mathbb{P}(S) + \sum_{k=2}^\infty \mathbb{P}(\emptyset)$$
    Subtracting $\mathbb{P}(S) = 1$ from both sides yields $\sum_{k=2}^\infty \mathbb{P}(\emptyset) = 0$. Because $\mathbb{P}(\emptyset) \ge 0$ by non-negativity (Axiom 1), it follows that $\mathbb{P}(\emptyset) = 0$. $\blacksquare$
* **Finite Additivity:** If $A_1, A_2, \dots, A_n \in \mathcal{F}$ are pairwise disjoint events (that is, $A_i \cap A_j = \emptyset$ whenever $i \ne j$), then:
  $$\mathbb{P}\left(\bigcup_{i=1}^n A_i\right) = \sum_{i=1}^n \mathbb{P}(A_i)$$
  * **Proof:** Extend the finite sequence to an infinite sequence by setting $A_k = \emptyset$ for all $k \ge n + 1$. Because $A_i \cap \emptyset = \emptyset$, the extended collection $\{A_k\}_{k=1}^\infty$ is mutually disjoint, and $\bigcup_{k=1}^\infty A_k = \bigcup_{i=1}^n A_i$. Applying Axiom 3 and $\mathbb{P}(\emptyset) = 0$:
    $$\mathbb{P}\left(\bigcup_{i=1}^n A_i\right) = \mathbb{P}\left(\bigcup_{k=1}^\infty A_k\right) = \sum_{k=1}^n \mathbb{P}(A_k) + \sum_{k=n+1}^\infty \mathbb{P}(\emptyset) = \sum_{i=1}^n \mathbb{P}(A_i)$$
    $\blacksquare$

###### **1.6.2 The Complement Rule**

* **Theorem (Complement Rule):** For any event $A \in \mathcal{F}$, the probability of its complement $A^c = S \setminus A$ satisfies:
  $$\mathbb{P}(A^c) = 1 - \mathbb{P}(A)$$
  * **Proof:** By definition, an outcome $\omega \in S$ either belongs to $A$ or does not belong to $A$. Hence, $A$ and $A^c$ form a partition of the sample space:
    $$A \cup A^c = S \quad \text{and} \quad A \cap A^c = \emptyset$$
    Applying finite additivity for two disjoint sets and normalization (Axiom 2):
    $$\mathbb{P}(S) = \mathbb{P}(A \cup A^c) = \mathbb{P}(A) + \mathbb{P}(A^c) = 1$$
    Rearranging terms gives $\mathbb{P}(A^c) = 1 - \mathbb{P}(A)$. $\blacksquare$

###### **1.6.3 Set Difference and Monotonicity**

* **Theorem (Set Difference):** For any two events $A, B \in \mathcal{F}$:
  1. If $A \subseteq B$, then:
     $$\mathbb{P}(B \setminus A) = \mathbb{P}(B) - \mathbb{P}(A)$$
  2. In general, for arbitrary events $A$ and $B$:
     $$\mathbb{P}(A \setminus B) = \mathbb{P}(A) - \mathbb{P}(A \cap B)$$
  * **Proof:** 
    1. If $A \subseteq B$, the set $B$ can be partitioned into the disjoint union of $A$ and the relative complement $B \setminus A = B \cap A^c$:
       $$B = A \cup (B \setminus A), \quad \text{where } A \cap (B \setminus A) = \emptyset$$
       By finite additivity:
       $$\mathbb{P}(B) = \mathbb{P}(A) + \mathbb{P}(B \setminus A) \implies \mathbb{P}(B \setminus A) = \mathbb{P}(B) - \mathbb{P}(A)$$
    2. For arbitrary $A$ and $B$, $A$ decomposes into the disjoint union $A = (A \setminus B) \cup (A \cap B)$. Since $(A \setminus B) \cap (A \cap B) = \emptyset$ and $(A \cap B) \subseteq A$, part (1) gives:
       $$\mathbb{P}(A \setminus B) = \mathbb{P}(A) - \mathbb{P}(A \cap B)$$
    $\blacksquare$
* **Theorem (Monotonicity):** If $A \subseteq B$, then:
  $$\mathbb{P}(A) \le \mathbb{P}(B)$$
  * **Proof:** From the partition $B = A \cup (B \setminus A)$, finite additivity yields $\mathbb{P}(B) = \mathbb{P}(A) + \mathbb{P}(B \setminus A)$. By Axiom 1 (Non-negativity), $\mathbb{P}(B \setminus A) \ge 0$. Therefore:
    $$\mathbb{P}(B) = \mathbb{P}(A) + \mathbb{P}(B \setminus A) \ge \mathbb{P}(A)$$
    $\blacksquare$
* **Corollary (Probability Bounds):** For any event $A \in \mathcal{F}$:
  $$0 \le \mathbb{P}(A) \le 1$$
  * **Proof:** Since $\emptyset \subseteq A \subseteq S$, monotonicity implies $\mathbb{P}(\emptyset) \le \mathbb{P}(A) \le \mathbb{P}(S)$. Substituting $\mathbb{P}(\emptyset) = 0$ and $\mathbb{P}(S) = 1$ yields $0 \le \mathbb{P}(A) \le 1$. $\blacksquare$

###### **1.6.4 Boole's Inequality (The Union Bound)**

* **Theorem (Boole's Inequality):** For any finite or countable sequence of events $A_1, A_2, \dots, A_n \in \mathcal{F}$ (not necessarily disjoint):
  $$\mathbb{P}\left(\bigcup_{i=1}^n A_i\right) \le \sum_{i=1}^n \mathbb{P}(A_i)$$
  * **Proof (by induction on $n$):** 
    For $n = 1$, the statement holds trivially with equality. 
    For $n = 2$, express $A_1 \cup A_2$ as the disjoint union $A_1 \cup (A_2 \setminus A_1)$. By additivity and the set difference identity:
    $$\mathbb{P}(A_1 \cup A_2) = \mathbb{P}(A_1) + \mathbb{P}(A_2 \setminus A_1) = \mathbb{P}(A_1) + \mathbb{P}(A_2) - \mathbb{P}(A_1 \cap A_2)$$
    Since $\mathbb{P}(A_1 \cap A_2) \ge 0$ by Axiom 1, subtracting it cannot increase the value:
    $$\mathbb{P}(A_1 \cup A_2) \le \mathbb{P}(A_1) + \mathbb{P}(A_2)$$
    Assuming the statement holds for $n - 1$ events, define $B = \bigcup_{i=1}^{n-1} A_i$. Then $\bigcup_{i=1}^n A_i = B \cup A_n$. Applying the base case and induction hypothesis:
    $$\mathbb{P}\left(\bigcup_{i=1}^n A_i\right) \le \mathbb{P}(B) + \mathbb{P}(A_n) \le \sum_{i=1}^{n-1} \mathbb{P}(A_i) + \mathbb{P}(A_n) = \sum_{i=1}^n \mathbb{P}(A_i)$$
    $\blacksquare$

###### **1.6.5 Why These Properties Matter**

The Kolmogorov axioms alone do not provide direct formulas for compound events. The complement rule converts complex "at least one" calculations into straightforward "none" calculations. The monotonicity and difference theorems allow probabilities of complex configurations to be bounded or decomposed into non-overlapping pieces. Boole's inequality provides an upper bound on failure or risk probabilities even when the dependency or overlap structure between components is entirely unknown.

###### **1.6.6 Worked Mini-Illustration**

Consider a system where two diagnostic checks, $A$ and $B$, evaluate an engine. Suppose:
$$\mathbb{P}(A) = 0.40, \quad \mathbb{P}(B) = 0.50, \quad \mathbb{P}(A \cap B) = 0.20$$

1. **Set Difference (Check $A$ triggers but Check $B$ does not):**
   $$\mathbb{P}(A \setminus B) = \mathbb{P}(A) - \mathbb{P}(A \cap B) = 0.40 - 0.20 = 0.20$$
2. **Complement Rule (Check $A$ does not trigger):**
   $$\mathbb{P}(A^c) = 1 - \mathbb{P}(A) = 1 - 0.40 = 0.60$$
3. **Monotonicity Check:**
   Because $(A \cap B) \subseteq A \subseteq S$, we observe:
   $$\mathbb{P}(A \cap B) = 0.20 \le \mathbb{P}(A) = 0.40 \le \mathbb{P}(S) = 1.00$$
   consistent with the monotonicity property.
4. **Union Bound vs. Exact Union:**
   By Boole's inequality, an upper bound on at least one check triggering is:
   $$\mathbb{P}(A \cup B) \le \mathbb{P}(A) + \mathbb{P}(B) = 0.40 + 0.50 = 0.90$$
   The exact probability accounts for overlap:
   $$\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B) - \mathbb{P}(A \cap B) = 0.40 + 0.50 - 0.20 = 0.70 \le 0.90$$

###### **1.6.7 Pitfalls and Common Misconceptions**

* **Invalid Set Difference Subtraction:** Writing $\mathbb{P}(A \setminus B) = \mathbb{P}(A) - \mathbb{P}(B)$. This equality holds **if and only if** $B \subseteq A$. If $B \not\subseteq A$, $\mathbb{P}(B)$ subtracts probability mass that was never in $A$, potentially producing an incorrect or negative result. The general formula is strictly $\mathbb{P}(A \setminus B) = \mathbb{P}(A) - \mathbb{P}(A \cap B)$.
* **Equating Additivity with Union Bound:** Assuming $\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B)$ always holds. Plain summation is valid only when $A \cap B = \emptyset$. For intersecting events, summation overcounts the overlap, giving an upper bound rather than an exact equality.
* **Confusing $\mathbb{P}(A) = 0$ with $A = \emptyset$:** The empty set always satisfies $\mathbb{P}(\emptyset) = 0$. However, the converse is false in infinite or continuous sample spaces: an event with probability zero is not necessarily empty (for instance, choosing a single specific real number from $[0, 1]$ under a uniform distribution has probability 0, but the corresponding singleton set is non-empty).

##### **1.7 Addition Rules and the Principle of Inclusion-Exclusion**

Kolmogorov's third axiom establishes additivity strictly for pairwise disjoint events. When analyzing real-world systems, events frequently overlap. The **addition rule** and the **Principle of Inclusion-Exclusion (PIE)** extend probability calculus to arbitrary unions by correcting for the probability mass of intersecting regions that would otherwise be counted multiple times.

###### **1.7.1 The Addition Rule for Two Events**

**Theorem (Addition Rule for Two Events).**  
For any two events $A, B \in \mathcal{F}$ in a probability space $(S, \mathcal{F}, \mathbb{P})$, the probability of their union is:
$$\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B) - \mathbb{P}(A \cap B)$$

*Proof.*  
Express the union $A \cup B$ as the union of two mutually disjoint events:
$$A \cup B = A \cup (B \setminus A)$$
Since $A \cap (B \setminus A) = \emptyset$, by Axiom 3 (finite additivity):
$$\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B \setminus A)$$
Similarly, the event $B$ can be partitioned into two disjoint subsets:
$$B = (A \cap B) \cup (B \setminus A)$$
Because $(A \cap B) \cap (B \setminus A) = \emptyset$, applying additivity yields:
$$\mathbb{P}(B) = \mathbb{P}(A \cap B) + \mathbb{P}(B \setminus A) \implies \mathbb{P}(B \setminus A) = \mathbb{P}(B) - \mathbb{P}(A \cap B)$$
Substituting this expression for $\mathbb{P}(B \setminus A)$ into the formula for $\mathbb{P}(A \cup B)$ gives:
$$\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B) - \mathbb{P}(A \cap B) \quad \blacksquare$$

Alternatively, partitioning the sample space covered by $A \cup B$ into three mutually exclusive regions gives:
$$A \cup B = (A \setminus B) \sqcup (A \cap B) \sqcup (B \setminus A)$$
Summing their probabilities:
$$\mathbb{P}(A \cup B) = \mathbb{P}(A \setminus B) + \mathbb{P}(A \cap B) + \mathbb{P}(B \setminus A)$$
Adding and subtracting $\mathbb{P}(A \cap B)$ reconstructs $\mathbb{P}(A) = \mathbb{P}(A \setminus B) + \mathbb{P}(A \cap B)$ and $\mathbb{P}(B) = \mathbb{P}(B \setminus A) + \mathbb{P}(A \cap B)$, which recovers the identical formula.

**Corollary (Mutually Exclusive Events).**  
If $A$ and $B$ are **mutually exclusive** (disjoint), then $A \cap B = \emptyset$. Since $\mathbb{P}(\emptyset) = 0$, the theorem simplifies directly to Axiom 3:
$$\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B)$$

###### **1.7.2 Principle of Inclusion-Exclusion for Three Events**

**Theorem (Inclusion-Exclusion for Three Events).**  
For any three events $A, B, C \in \mathcal{F}$, the probability that **at least one** event occurs is:
$$\mathbb{P}(A \cup B \cup C) = \mathbb{P}(A) + \mathbb{P}(B) + \mathbb{P}(C) - \mathbb{P}(A \cap B) - \mathbb{P}(A \cap C) - \mathbb{P}(B \cap C) + \mathbb{P}(A \cap B \cap C)$$

*Proof.*  
Treat $B \cup C$ as a single compound event $D = B \cup C$. Applying the two-event addition rule:
$$\mathbb{P}(A \cup D) = \mathbb{P}(A) + \mathbb{P}(D) - \mathbb{P}(A \cap D)$$
Expanding $\mathbb{P}(D) = \mathbb{P}(B \cup C)$:
$$\mathbb{P}(D) = \mathbb{P}(B) + \mathbb{P}(C) - \mathbb{P}(B \cap C)$$
Using the distributive law of set operations, $A \cap D = A \cap (B \cup C) = (A \cap B) \cup (A \cap C)$. Applying the two-event addition rule to this intersection:
$$\mathbb{P}(A \cap D) = \mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) - \mathbb{P}((A \cap B) \cap (A \cap C))$$
Since $(A \cap B) \cap (A \cap C) = A \cap B \cap C$:
$$\mathbb{P}(A \cap D) = \mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) - \mathbb{P}(A \cap B \cap C)$$
Substituting $\mathbb{P}(D)$ and $\mathbb{P}(A \cap D)$ back into the initial expansion:
$$\mathbb{P}(A \cup B \cup C) = \mathbb{P}(A) + \mathbb{P}(B) + \mathbb{P}(C) - \mathbb{P}(B \cap C) - \left[\mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) - \mathbb{P}(A \cap B \cap C)\right]$$
Rearranging terms completes the proof. $\blacksquare$

---

###### **1.7.3 Compound Events Derived from Three Sets**

The Venn partition of three overlapping events divides the union into seven mutually disjoint regions: three single-event regions, three two-event intersection regions, and one triple-intersection region. From these, probabilities of standard compound configurations are systematically evaluated:

1. **Probability of "None of the events occur":**  
   By De Morgan's laws, the event that none occur is the complement of at least one occurring:
   $$\mathbb{P}(A^c \cap B^c \cap C^c) = \mathbb{P}\left((A \cup B \cup C)^c\right) = 1 - \mathbb{P}(A \cup B \cup C)$$
2. **Probability of "At least two of three occur":**  
   Consider the pairwise intersections $\sum_{i<j} \mathbb{P}(A_i \cap A_j) = \mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) + \mathbb{P}(B \cap C)$. Any sample point belonging to *exactly two* events is included once in this sum. However, a point belonging to *all three* events ($A \cap B \cap C$) belongs to all three pairwise intersections and is counted $3$ times. To include it exactly once in the event "at least two occur", subtract twice the triple intersection:
$$\mathbb{P}(A \cap D) = \mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) - \mathbb{P}((A \cap B) \cap (A \cap C))$$
Since $(A \cap B) \cap (A \cap C) = A \cap B \cap C$:
$$\mathbb{P}(A \cap D) = \mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) - \mathbb{P}(A \cap B \cap C)$$
Substituting $\mathbb{P}(D)$ and $\mathbb{P}(A \cap D)$ back into the initial expansion:
$$\mathbb{P}(A \cup B \cup C) = \mathbb{P}(A) + \mathbb{P}(B) + \mathbb{P}(C) - \mathbb{P}(B \cap C) - \left[\mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) - \mathbb{P}(A \cap B \cap C)\right]$$
Rearranging terms completes the proof. $\blacksquare$

---

###### **1.7.3 Compound Events Derived from Three Sets**

The Venn partition of three overlapping events divides the union into seven mutually disjoint regions: three single-event regions, three two-event intersection regions, and one triple-intersection region. From these, probabilities of standard compound configurations are systematically evaluated:

1. **Probability of "None of the events occur":**  
   By De Morgan's laws, the event that none occur is the complement of at least one occurring:
   $$\mathbb{P}(A^c \cap B^c \cap C^c) = \mathbb{P}\left((A \cup B \cup C)^c\right) = 1 - \mathbb{P}(A \cup B \cup C)$$
2. **Probability of "At least two of three occur":**  
   Consider the pairwise intersections $\sum_{i<j} \mathbb{P}(A_i \cap A_j) = \mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) + \mathbb{P}(B \cap C)$. Any sample point belonging to *exactly two* events is included once in this sum. However, a point belonging to *all three* events ($A \cap B \cap C$) belongs to all three pairwise intersections and is counted $3$ times. To include it exactly once in the event "at least two occur", subtract twice the triple intersection:
   $$\mathbb{P}(\text{at least two}) = \mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) + \mathbb{P}(B \cap C) - 2\,\mathbb{P}(A \cap B \cap C)$$
3. **Probability of "Exactly two of three occur":**  
   To isolate the outcomes belonging to exactly two sets without the central core:
   $$\mathbb{P}(\text{exactly two}) = \mathbb{P}(\text{at least two}) - \mathbb{P}(A \cap B \cap C) = \sum_{i<j} \mathbb{P}(A_i \cap A_j) - 3\,\mathbb{P}(A \cap B \cap C)$$
4. **Probability of "Exactly one of three occurs":**  
   The event that exactly one occurs consists of the entire union excluding all outcomes that lie in two or more events:
   $$\mathbb{P}(\text{exactly one}) = \mathbb{P}(A \cup B \cup C) - \mathbb{P}(\text{at least two})$$

---

###### **1.7.4 Worked Illustrations**

**Example 1 (Two Events: Academic Exam Performance).**  
A student sits for examinations in Calculus ($M$) and Linear Algebra ($L$). Historical records show $\mathbb{P}(M) = 0.7$, $\mathbb{P}(L) = 0.6$, and the probability of passing both is $\mathbb{P}(M \cap L) = 0.5$.
- *Passes at least one exam:*  
  $$\mathbb{P}(M \cup L) = \mathbb{P}(M) + \mathbb{P}(L) - \mathbb{P}(M \cap L) = 0.7 + 0.6 - 0.5 = 0.8$$
- *Passes neither exam:*  
  $$\mathbb{P}(M^c \cap L^c) = 1 - \mathbb{P}(M \cup L) = 1 - 0.8 = 0.2$$
- *Passes Calculus but fails Linear Algebra:*  
  $$\mathbb{P}(M \setminus L) = \mathbb{P}(M) - \mathbb{P}(M \cap L) = 0.7 - 0.5 = 0.2$$
- *Passes exactly one exam (symmetric difference $M \Delta L$):*  
  $$\mathbb{P}(M \Delta L) = \mathbb{P}(M \cup L) - \mathbb{P}(M \cap L) = 0.8 - 0.5 = 0.3$$

**Example 2 (Three Events: System Diagnostics).**  
Three monitoring modules $A, B, C$ satisfy:
$$\mathbb{P}(A) = \mathbb{P}(B) = \mathbb{P}(C) = 0.5$$
$$\mathbb{P}(A \cap B) = \mathbb{P}(A \cap C) = 0.3, \quad \mathbb{P}(B \cap C) = 0.2, \quad \mathbb{P}(A \cap B \cap C) = 0.1$$
- *At least one module triggers:*  
  $$\mathbb{P}(A \cup B \cup C) = (0.5 + 0.5 + 0.5) - (0.3 + 0.3 + 0.2) + 0.1 = 1.5 - 0.8 + 0.1 = 0.8$$
- *None trigger:*  
  $$\mathbb{P}(A^c \cap B^c \cap C^c) = 1 - 0.8 = 0.2$$
- *At least two trigger:*  
  $$\mathbb{P}(\text{at least two}) = (0.3 + 0.3 + 0.2) - 2(0.1) = 0.8 - 0.2 = 0.6$$
- *Exactly one triggers:*  
  $$\mathbb{P}(\text{exactly one}) = \mathbb{P}(A \cup B \cup C) - \mathbb{P}(\text{at least two}) = 0.8 - 0.6 = 0.2$$

*Sanity Check (Partition of Sample Space):*  
$$\mathbb{P}(\text{none}) + \mathbb{P}(\text{exactly one}) + \mathbb{P}(\text{exactly two}) + \mathbb{P}(\text{all three}) = 0.2 + 0.2 + (0.6 - 0.1) + 0.1 = 1.0$$

---

###### **1.7.5 Common Pitfalls and Boundary Checks**

* **Blindly adding marginals:** Summing $\mathbb{P}(A) + \mathbb{P}(B)$ without verifying disjointness can lead to values greater than $1$. For example, if John receives job offers from company $A$ with probability $0.8$ and company $B$ with probability $0.6$, adding them gives $1.4$, violating Axiom 2. The overlap $\mathbb{P}(A \cap B) = 0.5$ must be subtracted: $0.8 + 0.6 - 0.5 = 0.9$.
* **Overcounting the triple intersection:** When computing "at least two of three", subtracting $\mathbb{P}(A \cap B \cap C)$ only once leaves the triple intersection counted twice (since it appears in each of the three pairwise sums). Subtracting $2\,\mathbb{P}(A \cap B \cap C)$ ensures each element in the central region is counted with net weight $3 - 2 = 1$.
* **Sign alternation in PIE:** In the inclusion-exclusion expansion for $n$ events, signs strictly alternate: $+ \sum \mathbb{P}(A_i) - \sum \mathbb{P}(A_i \cap A_j) + \sum \mathbb{P}(A_i \cap A_j \cap A_k) - \dots$. The three-event formula must terminate with $+ \mathbb{P}(A \cap B \cap C)$, not a negative sign.

##### **1.8 Classical Probability and Discrete Weight Assignments**

A **discrete sample space** $S$ is a sample space that is either finite, $S = \{\omega_1, \omega_2, \dots, \omega_N\}$, or countably infinite, $S = \{\omega_1, \omega_2, \dots\}$. On such spaces, the event space is conventionally chosen as the power set $\mathcal{F} = 2^S$. By Kolmogorov's countable additivity axiom, assigning a probability measure $\mathbb{P}$ on $(S, \mathcal{F})$ is entirely equivalent to assigning a set of non-negative real numbers—called **elementary weights** or **point probabilities**—$p(\omega) = \mathbb{P}(\{\omega\})$ to every individual singleton outcome $\omega \in S$.

###### **1.8.1 Discrete Probability Measures and Weight Normalization**

A function $p: S \to \mathbb{R}$ defines a valid **discrete probability distribution** on $S$ if and only if it satisfies two fundamental criteria:
1. **Non-negativity:** 
   $$p(\omega) \ge 0 \quad \forall \, \omega \in S$$
2. **Normalization:** 
   $$\sum_{\omega \in S} p(\omega) = 1$$

For any event $A \subseteq S$, its probability is computed by summing the weights of its constituent outcomes:
$$\mathbb{P}(A) = \sum_{\omega \in A} p(\omega) = \sum_{\omega \in A} \mathbb{P}(\{\omega\})$$

*Why it matters:* Discrete weight assignments reduce the abstract measure-theoretic definition of probability to elementary summation. Once weights are assigned to singletons, the probability of any arbitrary event is uniquely determined without needing integration or measure extensions.

###### **1.8.2 The Classical (Laplace) Probability Model**

The **classical probability model** (or Laplace model) applies when a random experiment has a finite sample space $S$ containing $N = |S|$ distinct outcomes that are assumed to be **equally likely** (symmetric). 

Under the principle of indifference, each elementary outcome receives an identical weight:
$$\mathbb{P}(\{\omega\}) = \frac{1}{N} = \frac{1}{|S|} \quad \forall \, \omega \in S$$

For any event $A \subseteq S$ consisting of $m = |A|$ favorable outcomes, the probability of $A$ reduces to the ratio of cardinalities:
$$\mathbb{P}(A) = \sum_{\omega \in A} \frac{1}{N} = \frac{|A|}{|S|} = \frac{m}{N} = \frac{\text{Number of favorable outcomes}}{\text{Total number of possible outcomes}}$$

**Worked Illustration: Hypergeometric Sampling Without Replacement**  
Consider an inspection batch of $N = 10$ items containing exactly $K = 3$ defective items and $N - K = 7$ non-defective items. A quality engineer draws a sample of $n = 4$ items uniformly at random without replacement. 

The total number of equally likely sample configurations is the number of 4-combinations from 10 items:
$$|S| = \binom{10}{4} = \frac{10 \times 9 \times 8 \times 7}{4 \times 3 \times 2 \times 1} = 210$$

Let $A_k$ denote the event that exactly $k$ defective items are selected in the sample. By combinatorial product rule, the number of ways to select $k$ defectives from $K$ and $n-k$ non-defectives from $N-K$ yields the classical hypergeometric formula:
$$\mathbb{P}(A_k) = \frac{\binom{K}{k}\binom{N-K}{n-k}}{\binom{N}{n}}$$

1. **Probability of zero defectives ($k=0$):**
   $$\mathbb{P}(A_0) = \frac{\binom{3}{0}\binom{7}{4}}{\binom{10}{4}} = \frac{1 \times 35}{210} = \frac{1}{6} \approx 0.1667$$
2. **Probability of exactly one defective ($k=1$):**
   $$\mathbb{P}(A_1) = \frac{\binom{3}{1}\binom{7}{3}}{\binom{10}{4}} = \frac{3 \times 35}{210} = \frac{105}{210} = \frac{1}{2} = 0.5$$
3. **Probability of at least one defective:** Using the complement rule avoids summing $k=1, 2, 3$:
   $$\mathbb{P}(\text{at least one defective}) = 1 - \mathbb{P}(A_0) = 1 - \frac{1}{6} = \frac{5}{6} \approx 0.8333$$

###### **1.8.3 Non-Uniform Discrete Weight Assignments**

When outcomes are not equally likely, probability cannot be computed by simply dividing cardinalities $|A|/|S|$. Instead, individual outcomes carry unequal weights determined by physical constraints or parameterizations.

**Worked Illustration: A Loaded Die with Linear Constraints**  
Consider rolling a six-sided die with sample space $S = \{1, 2, 3, 4, 5, 6\}$. The die is biased such that each even face is twice as likely to turn up as each odd face. 

Let the probability of obtaining any odd number be $w \ge 0$:
$$\mathbb{P}(\{1\}) = \mathbb{P}(\{3\}) = \mathbb{P}(\{5\}) = w$$
Then each even outcome has probability:
$$\mathbb{P}(\{2\}) = \mathbb{P}(\{4\}) = \mathbb{P}(\{6\}) = 2w$$
By the normalization condition:
$$\sum_{k=1}^{6} \mathbb{P}(\{k\}) = 3(w) + 3(2w) = 9w = 1 \implies w = \frac{1}{9}$$

The point probabilities are therefore $\mathbb{P}(\{\text{odd}\}) = 1/9$ and $\mathbb{P}(\{\text{even}\}) = 2/9$. 

Now, define two events:

* $A = \{2, 4, 6\}$ (an even number occurs)
* $B = \{3, 6\}$ (a number divisible by 3 occurs)

We evaluate:
$$\mathbb{P}(A \cap B) = \mathbb{P}(\{6\}) = \frac{2}{9}$$
$$\mathbb{P}(A \cup B) = \mathbb{P}(\{2, 3, 4, 6\}) = \mathbb{P}(\{2\}) + \mathbb{P}(\{3\}) + \mathbb{P}(\{4\}) + \mathbb{P}(\{6\}) = \frac{2}{9} + \frac{1}{9} + \frac{2}{9} + \frac{2}{9} = \frac{7}{9}$$
Verifying with the general addition rule:
$$\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B) - \mathbb{P}(A \cap B) = \frac{6}{9} + \frac{3}{9} - \frac{2}{9} = \frac{7}{9}$$

###### **1.8.4 Countably Infinite Probability Distributions**

When $S$ is countably infinite (such as $S = \{1, 2, 3, \dots\} = \mathbb{N}$), weights must be assigned via convergent infinite series to ensure that normalization $\sum_{k=1}^{\infty} p(k) = 1$ holds.

**Worked Illustration: Geometric Weight Distribution**  
Let $S = \{1, 2, 3, \dots\}$ with singleton probabilities defined by the geometric law:
$$\mathbb{P}(\{k\}) = \frac{c}{3^k}, \quad k \in \mathbb{N}$$
where $c$ is a normalization constant.

1. **Solving for the normalization constant $c$:**  
   Applying the normalization axiom and the infinite geometric series sum formula $\sum_{k=1}^\infty q^k = \frac{q}{1-q}$ (for $|q| < 1$ with $q = 1/3$):
   $$\mathbb{P}(S) = \sum_{k=1}^{\infty} \frac{c}{3^k} = c \sum_{k=1}^{\infty} \left(\frac{1}{3}\right)^k = c \cdot \frac{1/3}{1 - 1/3} = c \cdot \frac{1/3}{2/3} = \frac{c}{2}$$
   Setting $\mathbb{P}(S) = 1$ gives:
   $$\frac{c}{2} = 1 \implies c = 2$$
   Since $p(k) = 2/3^k > 0$ for all $k \ge 1$, non-negativity is preserved, and the measure is valid.
2. **Probability of an even outcome:**  
   The event $E_{\text{even}} = \{2, 4, 6, \dots\} = \{2m : m \in \mathbb{N}\}$. The common ratio of consecutive terms in the series is $(1/3)^2 = 1/9$:
   $$\mathbb{P}(E_{\text{even}}) = \sum_{m=1}^{\infty} \mathbb{P}(\{2m\}) = \sum_{m=1}^{\infty} \frac{2}{3^{2m}} = 2 \sum_{m=1}^{\infty} \left(\frac{1}{9}\right)^m = 2 \cdot \frac{1/9}{1 - 1/9} = 2 \cdot \frac{1/9}{8/9} = 2 \cdot \frac{1}{8} = \frac{1}{4}$$
3. **Tail probability $\mathbb{P}(k \ge 3)$:**  
   Using the complement rule is significantly faster than summing the infinite tail:
   $$\mathbb{P}(\{k \ge 3\}) = 1 - \mathbb{P}(\{1\}) - \mathbb{P}(\{2\}) = 1 - \frac{2}{3^1} - \frac{2}{3^2} = 1 - \frac{2}{3} - \frac{2}{9} = 1 - \frac{8}{9} = \frac{1}{9}$$

###### **1.8.5 Common Pitfalls and Boundary Checks**

* **The Equal-Likelihood Trap:** Assuming $\mathbb{P}(A) = |A|/|S|$ when the physical system is inherently biased. For instance, claiming that tossing two coins has sample space $\{\text{0 Heads}, \text{1 Head}, \text{2 Heads}\}$ and therefore $\mathbb{P}(\text{1 Head}) = 1/3$ is incorrect: the underlying symmetric elementary outcomes are $\{TT, HT, TH, HH\}$, giving a weight of $2/4 = 1/2$ to the outcome of 1 Head.
* **Failure of Additivity with Non-Linear Weight Formulas:** Assigning set probabilities using non-linear functions of set cardinalities—such as setting $\mathbb{P}(A) = (|A|/N)^2$—violates finite additivity. For disjoint singletons $\{1\}$ and $\{2\}$ on $S = \{1, 2, 3, 4\}$, $\mathbb{P}(\{1\}) + \mathbb{P}(\{2\}) = (1/4)^2 + (1/4)^2 = 1/8$, whereas $\mathbb{P}(\{1, 2\}) = (2/4)^2 = 1/4 \neq 1/8$.
* **Finite vs. Countable Additivity on Infinite Sets:** Defining an assignment such that $Q(A) = 0$ for all finite subsets $A \subset \mathbb{N}$ and $Q(A) = 1$ for all infinite subsets satisfies non-negativity and normalization ($Q(\mathbb{N}) = 1$), but violates countable additivity: $\mathbb{N} = \bigcup_{k=1}^\infty \{k\}$ would yield $\sum_{k=1}^\infty Q(\{k\}) = \sum 0 = 0 \neq Q(\mathbb{N})$. Individual point weights must converge absolutely to 1 over countable unions.

##### **1.9 Geometric Probability**

**Geometric probability** is the assignment of probabilities to continuous sample spaces embedded in Euclidean space $\mathbb{R}^n$ by means of geometric measure (length, area, or volume). 

Let the sample space $S \subset \mathbb{R}^n$ be a measurable set with finite, strictly positive $n$-dimensional Lebesgue measure $\mu(S) \in (0, \infty)$. If all points in $S$ are equally likely—meaning the likelihood of an outcome falling into any subregion depends only on its measure and not on its location or shape—the **geometric probability** of an event $A \subseteq S$ (where $A$ is measurable) is defined as:

$$\mathbb{P}(A) = \frac{\mu(A)}{\mu(S)} = \frac{\text{measure}(A)}{\text{measure}(S)}$$

where the measure represents:

* **Length** ($\mu = \text{length}$) when $S \subset \mathbb{R}$,
* **Area** ($\mu = \text{area}$) when $S \subset \mathbb{R}^2$,
* **Volume** ($\mu = \text{volume}$) when $S \subset \mathbb{R}^3$.

###### **1.9.1 Why Geometric Probability Matters**
In continuous sample spaces (such as continuous time intervals or spatial regions), the number of possible outcomes is uncountably infinite. Assigning a discrete probability mass to each individual point leads to an immediate contradiction: if each singleton $\{\omega\}$ had a positive weight $\epsilon > 0$, the sum over uncountably many disjoint points would diverge to $\infty$; conversely, if $\mathbb{P}(\{\omega\}) = 0$ for all $\omega$, classical counting ratio $\frac{|A|}{|S|}$ becomes the indeterminate form $\frac{\infty}{\infty}$. 

Geometric probability resolves this by using continuous geometric measures instead of cardinality, serving as the continuous counterpart to Laplace's classical model of equally likely outcomes.

###### **1.9.2 The Meeting Problem**

A classic application in $\mathbb{R}^2$ is the rendezvous problem.

Two individuals, Alice and Bob, agree to meet at a specified location between 12:00 and 13:00. Each arrives independently at a uniformly distributed time during the hour. Whoever arrives first waits for exactly $t$ minutes ($0 \le t \le 60$) and leaves if the other has not arrived. What is the probability that they successfully meet?

**1. Coordinate Formulation and Sample Space**  
Let $x \in [0, 60]$ denote Alice's arrival time in minutes past 12:00, and let $y \in [0, 60]$ denote Bob's arrival time. Since the arrivals are independent and uniform, the sample space is the square region:

$$S = \{(x, y) \in \mathbb{R}^2 : 0 \le x \le 60,\ 0 \le y \le 60\}$$

$$\text{area}(S) = 60 \times 60 = 3600$$

**2. Condition for Meeting**  
Alice and Bob meet if and only if the arrival difference is at most $t$:

$$|x - y| \le t \iff -t \le y - x \le t \iff x - t \le y \le x + t$$

The meeting event $A_t$ corresponds geometrically to the diagonal strip bounded by the lines $y = x + t$ and $y = x - t$ inside the square $[0, 60]^2$.

**3. Complementary Area Computation**  
The favorable region $A_t$ is a hexagon. Computing its area directly is cumbersome, but its complement $A_t^c$ (the event that they do not meet) decomposes cleanly into two disjoint, symmetric right isosceles triangles in the top-left and bottom-right corners:

* Upper triangle: $y - x > t$, with vertices $(0, t)$, $(0, 60)$, and $(60 - t, 60)$. Both legs have length $60 - t$.
* Lower triangle: $x - y > t$, with vertices $(t, 0)$, $(60, 0)$, and $(60, 60 - t)$. Both legs have length $60 - t$.

$$\text{area}(A_t^c) = 2 \times \left(\frac{1}{2} (60 - t)^2\right) = (60 - t)^2$$

Subtracting the complementary area from $\text{area}(S)$:

$$\text{area}(A_t) = 3600 - (60 - t)^2$$

$$\mathbb{P}(A_t) = \frac{3600 - (60 - t)^2}{3600} = 1 - \left(\frac{60 - t}{60}\right)^2$$

**Concrete Evaluation for $t = 15$ Minutes:**  
If each waits $t = 15$ minutes:

$$\text{area}(A_{15}^c) = (60 - 15)^2 = 45^2 = 2025$$

$$\text{area}(A_{15}) = 3600 - 2025 = 1575$$

$$\mathbb{P}(A_{15}) = \frac{1575}{3600} = \frac{7}{16} = 0.4375$$

Alice and Bob have a $43.75\%$ chance of meeting.

**Inverse Problem (Meeting Threshold):**  
How long must they agree to wait to guarantee that $\mathbb{P}(A_t) \ge \frac{1}{2}$?

$$1 - \left(\frac{60 - t}{60}\right)^2 \ge \frac{1}{2} \iff \left(\frac{60 - t}{60}\right)^2 \le \frac{1}{2} \iff 60 - t \le \frac{60}{\sqrt{2}} = 30\sqrt{2}$$

$$t \ge 60 - 30\sqrt{2} \approx 60 - 42.426 = 17.574 \text{ minutes}$$

They must wait at least approximately $17\text{ minutes and } 35\text{ seconds}$.

###### **1.9.3 The Broken Stick Problem**
A stick of length $1$ is broken at two points chosen independently and uniformly at random along its length. What is the probability that the three resulting segments can form a triangle?

**1. Algebraic Reduction via Triangle Inequality**  
Let $x, y \in [0, 1]$ be the two break points. The sample space is the unit square $S = [0, 1]^2$ with $\text{area}(S) = 1$.

Three positive segments $a, b, c$ with $a + b + c = 1$ form a non-degenerate triangle if and only if the triangle inequalities hold: $a + b > c$, $a + c > b$, and $b + c > a$. Substituting $a + b = 1 - c$ into $a + b > c$ yields $1 - c > c \iff c < \frac{1}{2}$. Applying this across all three sides gives an equivalent condition:
$$\text{Three segments summing to } 1 \text{ form a triangle} \iff \text{each piece has length strictly less than } \frac{1}{2}.$$

**2. Geometric Partitioning**  
The diagonal line $y = x$ partitions $S$ into two open triangles of area $1/2$ (the event $x = y$ has Lebesgue measure zero):

* **Case 1: $x < y$.**  
  The three segment lengths are $a = x$, $b = y - x$, and $c = 1 - y$. The conditions become:
  1. $a < \frac{1}{2} \implies x < \frac{1}{2}$
  2. $b < \frac{1}{2} \implies y - x < \frac{1}{2} \implies y < x + \frac{1}{2}$
  3. $c < \frac{1}{2} \implies 1 - y < \frac{1}{2} \implies y > \frac{1}{2}$

  Combining these with $0 < x < y < 1$, the favorable region in the upper half-plane is bounded by $x = 0$, $y = \frac{1}{2}$, $x = \frac{1}{2}$, and $y = x + \frac{1}{2}$. This forms a right triangle with vertices:
  $$\left(0, \frac{1}{2}\right), \quad \left(\frac{1}{2}, \frac{1}{2}\right), \quad \left(\frac{1}{2}, 1\right)$$
  The base is $\frac{1}{2} - 0 = \frac{1}{2}$ and the height is $1 - \frac{1}{2} = \frac{1}{2}$. Its area is:
  $$\text{area}_1 = \frac{1}{2} \times \frac{1}{2} \times \frac{1}{2} = \frac{1}{8}$$
* **Case 2: $x > y$.**  
  By exact reflection across the line $y = x$, the segments are $y$, $x - y$, and $1 - x$. The inequalities yield the symmetric triangle with vertices:
  $$\left(\frac{1}{2}, 0\right), \quad \left(\frac{1}{2}, \frac{1}{2}\right), \quad \left(1, \frac{1}{2}\right)$$
  This region also has area:
  $$\text{area}_2 = \frac{1}{8}$$

**3. Total Probability**  
Because Cases 1 and 2 are disjoint, the total favorable area is:

$$\text{area}(A) = \text{area}_1 + \text{area}_2 = \frac{1}{8} + \frac{1}{8} = \frac{1}{4}$$

$$\mathbb{P}(\text{triangle}) = \frac{\text{area}(A)}{\text{area}(S)} = \frac{1/4}{1} = \frac{1}{4} = 0.25$$

###### **1.9.4 Pitfalls and Conceptual Nuances**

* **Zero Probability vs. Impossibility:** In continuous spaces, any single point $\omega = (x_0, y_0)$ has measure zero ($\mu(\{\omega\}) = 0$), so $\mathbb{P}(\{\omega\}) = 0$. However, an event with probability 0 is **not impossible**: when the experiment is executed, some exact point $\omega$ will inevitably occur. Impossibility strictly means $\omega \notin S$ (the empty set $\emptyset$), whereas $\mathbb{P}(A) = 0$ can occur for non-empty null sets.
* **Boundaries Do Not Affect Probability:** Because lines in $\mathbb{R}^2$ have zero area (and individual points in $\mathbb{R}$ have zero length), whether inequalities defining an event are strict ($<$) or non-strict ($\le$) does not alter the geometric probability:
  $$\mathbb{P}(|x - y| \le t) = \mathbb{P}(|x - y| < t)$$
* **Dimensional Inconsistency:** You can only divide measures of the same dimension. Dividing a 1D length boundary by a 2D sample space area produces meaningless units. The event $A$ must be defined as a subset of $S$ sharing the ambient dimension of $S$.
* **Uniformity Assumption:** The formula $\mathbb{P}(A) = \frac{\mu(A)}{\mu(S)}$ is valid **only** when outcomes are uniformly distributed over $S$. If arrival density or point distributions vary across the space, probability must instead be computed by integrating the probability density function over $A$.

***

#### **2. Definitions**

*   **Random Experiment**: An observational procedure or physical process whose outcome cannot be predicted with certainty prior to its execution.
*   **Outcome**: A single, indivisible, and elementary result that can be observed from the execution of a random experiment.
*   **Sample Space**: The set of all possible distinct outcomes of a random experiment, typically denoted by $S$ or $\Omega$.
*   **Event**: A subset of the sample space consisting of zero or more outcomes, which occurs whenever the realized experimental outcome belongs to that subset.
*   **Simple Event**: An event consisting of exactly one individual outcome from the sample space.
*   **Compound Event**: An event consisting of two or more distinct outcomes from the sample space.
*   **Certain Event**: The event comprising the entire sample space $S$, which is guaranteed to occur on every trial of an experiment.
*   **Impossible Event (Null Event)**: The empty subset $\emptyset$ of the sample space containing no outcomes, which cannot occur in any trial of the experiment.
*   **Mutually Exclusive (Disjoint) Events**: Events that have no outcomes in common, meaning their set intersection is the empty set and they cannot occur simultaneously.
*   **Union of Events**: The event $A \cup B$ consisting of all outcomes that belong to event $A$, to event $B$, or to both, representing the occurrence of at least one of the two events.
*   **Intersection of Events**: The event $A \cap B$ consisting of all outcomes common to both $A$ and $B$, representing the simultaneous occurrence of both events.
*   **Complement of an Event**: The event $A^c$ consisting of all outcomes in the sample space that do not belong to $A$, representing the event that $A$ does not occur.
*   **Set Difference of Events**: The event $A \setminus B$ containing all outcomes that belong to $A$ but do not belong to $B$.
*   **Symmetric Difference of Events**: The event $A \Delta B = (A \setminus B) \cup (B \setminus A)$ containing all outcomes that belong to either $A$ or $B$ but not to both.
*   **Empirical Probability**: The proportion of times an event occurs out of the total number of trials across a large number of independent experimental repetitions.
*   **Classical Probability**: The probability of an event defined on a finite sample space with equally likely outcomes as the ratio of the number of favorable outcomes to the total number of possible outcomes.
*   **Geometric Probability**: A continuous probability framework where the probability of an event is the ratio of its geometric measure (length, area, or volume) to the corresponding geometric measure of the entire sample space under a uniform distribution.
*   **$\sigma$-Algebra**: A collection $\mathcal{F}$ of subsets of a sample space $S$ that contains $S$ itself and is closed under set complementation and countable unions.
*   **Measurable Space**: A pair $(S, \mathcal{F})$ consisting of a sample space $S$ and an associated $\sigma$-algebra $\mathcal{F}$ specifying the collection of allowable events.
*   **Probability Measure**: A function $\mathbb{P}: \mathcal{F} \to [0, 1]$ on a $\sigma$-algebra $\mathcal{F}$ satisfying non-negativity, normalization ($\mathbb{P}(S) = 1$), and countable additivity for pairwise disjoint events.
*   **Probability Space**: An axiomatic triple $(S, \mathcal{F}, \mathbb{P})$ consisting of a sample space $S$, a $\sigma$-algebra of events $\mathcal{F}$, and a probability measure $\mathbb{P}$.
*   **Borel $\sigma$-Algebra**: The smallest $\sigma$-algebra on $\mathbb{R}$ that contains all open intervals, serving as the standard domain for continuous probability measures on the real line.

***

#### **3. Formulas**

*   **Empirical Probability (Relative Frequency)**: $\mathbb{P}(A) \approx \frac{m}{n}$, where $m$ is the number of times event $A$ occurs in $n$ independent and identical trials; converges to $\mathbb{P}(A)$ as $n \to \infty$.
*   **Classical Probability**: $\mathbb{P}(A) = \frac{|A|}{|S|} = \frac{m}{n}$, applicable when the sample space $S$ consists of a finite number $n$ of outcomes that are all mutually exclusive and equally likely, with $m = |A|$ favorable outcomes.
*   **Geometric Probability**: $\mathbb{P}(A) = \frac{\operatorname{measure}(A)}{\operatorname{measure}(S)}$, applicable when outcomes are chosen uniformly at random from a measurable region $S \subset \mathbb{R}^d$ ($d \in \{1, 2, 3\}$) with non-zero, finite Lebesgue measure (length, area, or volume), where $A \subseteq S$ is a measurable event.
*   **Axiom of Countable Additivity**: $\mathbb{P}\left(\bigcup_{i=1}^{\infty} A_i\right) = \sum_{i=1}^{\infty} \mathbb{P}(A_i)$, holds for any countable sequence of pairwise disjoint events $\{A_i\}_{i=1}^\infty \subset \mathcal{F}$ ($A_i \cap A_j = \emptyset$ for all $i \ne j$).
*   **Probability of the Impossible Event**: $\mathbb{P}(\emptyset) = 0$, valid on any probability space $(S, \mathcal{F}, \mathbb{P})$.
*   **Probability of the Complement**: $\mathbb{P}(A^c) = 1 - \mathbb{P}(A)$, valid for any event $A \in \mathcal{F}$.
*   **Monotonicity of Probability**: $\mathbb{P}(A) \le \mathbb{P}(B)$, holds for all events $A, B \in \mathcal{F}$ satisfying $A \subseteq B$.
*   **Difference Rule for Subsets**: $\mathbb{P}(B \setminus A) = \mathbb{P}(B) - \mathbb{P}(A)$, applicable whenever $A \subseteq B$.
*   **General Event Difference Rule**: $\mathbb{P}(A \setminus B) = \mathbb{P}(A) - \mathbb{P}(A \cap B)$, valid for any pair of events $A, B \in \mathcal{F}$.
*   **Addition Rule for Disjoint Events**: $\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B)$, applicable when $A$ and $B$ are mutually exclusive ($A \cap B = \emptyset$).
*   **Addition Rule for Two Arbitrary Events**: $\mathbb{P}(A \cup B) = \mathbb{P}(A) + \mathbb{P}(B) - \mathbb{P}(A \cap B)$, valid for any arbitrary events $A, B \in \mathcal{F}$.
*   **Inclusion–Exclusion Principle for Three Events**: $\mathbb{P}(A \cup B \cup C) = \mathbb{P}(A) + \mathbb{P}(B) + \mathbb{P}(C) - \mathbb{P}(A \cap B) - \mathbb{P}(A \cap C) - \mathbb{P}(B \cap C) + \mathbb{P}(A \cap B \cap C)$, valid for all events $A, B, C \in \mathcal{F}$.
*   **Probability of at Least Two Events (Three Events)**: $\mathbb{P}(\text{at least two of } A, B, C) = \mathbb{P}(A \cap B) + \mathbb{P}(A \cap C) + \mathbb{P}(B \cap C) - 2\mathbb{P}(A \cap B \cap C)$, valid for all events $A, B, C \in \mathcal{F}$.
*   **Probability of Exactly One Event (Three Events)**: $\mathbb{P}(\text{exactly one of } A, B, C) = \mathbb{P}(A \cup B \cup C) - \mathbb{P}(\text{at least two of } A, B, C)$, valid for all events $A, B, C \in \mathcal{F}$.
*   **Symmetric Difference Probability**: $\mathbb{P}(A \,\Delta\, B) = \mathbb{P}(A \cup B) - \mathbb{P}(A \cap B) = \mathbb{P}(A \setminus B) + \mathbb{P}(B \setminus A)$, representing the probability that exactly one of the two events $A$ or $B$ occurs.
*   **Union Bound (Boole's Inequality)**: $\mathbb{P}\left(\bigcup_{i=1}^n A_i\right) \le \sum_{i=1}^n \mathbb{P}(A_i)$, valid for any finite or countable collection of events $A_1, \dots, A_n \in \mathcal{F}$.
*   **De Morgan's Laws for Events**: $(A \cup B)^c = A^c \cap B^c$ and $(A \cap B)^c = A^c \cup B^c$, generally $\left(\bigcup_{i} A_i\right)^c = \bigcap_{i} A_i^c$ and $\left(\bigcap_{i} A_i\right)^c = \bigcup_{i} A_i^c$, holding for arbitrary collections of subsets of $S$.
*   **Cardinality of $\sigma$-Algebra Generated by a Finite Partition**: $|\sigma(\mathcal{P})| = 2^k$, applicable when $\mathcal{P} = \{E_1, E_2, \dots, E_k\}$ forms a partition of sample space $S$ into $k$ non-empty, pairwise disjoint events.

***

#### **4. Practice**

##### **4.1. Empirical Heads Frequency** (Lecture, Example 1)

A coin is tossed 1000 times and lands on Heads 510 times. Find the empirical probability of getting Heads.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Empirical probability estimates the true probability of an event as its relative frequency over a large sequence of repeated trials.

1.  **Extract the experimental counts.**
    *   The total number of trials is $n = 1000$.
    *   The number of favorable occurrences of Heads is $m = 510$.
    *   **Answer: Counts identified** — $n = 1000, m = 510$.
2.  **Compute the relative frequency.**
    *   Apply the empirical probability formula:
        $$P(\text{Heads}) \approx \frac{m}{n} = \frac{510}{1000} = 0.51.$$
    *   **Answer: Empirical probability** — $0.51$.

**Answer:** $\mathbf{0.51}$
</details>

##### **4.2. Die Toss Sample Spaces** (Lecture, Example 2)

Determine the sample space when recording the exact number showing on a tossed die, and compare it with the sample space when recording only whether the number is even or odd.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** A sample space depends entirely on the degree of detail observed in the experiment, with finer partitions retaining strictly more information.

1.  **Specify the sample space of exact face outcomes.**
    *   A standard six-sided die displays faces from 1 to 6.
    *   $S_1 = \{1, 2, 3, 4, 5, 6\}$.
    *   **Answer: Exact sample space** — $S_1 = \{1, 2, 3, 4, 5, 6\}$.
2.  **Specify the sample space of parity outcomes.**
    *   Recording only parity reduces the possible observed outcomes to two categories.
    *   $S_2 = \{\text{even}, \text{odd}\}$.
    *   **Answer: Parity sample space** — $S_2 = \{\text{even}, \text{odd}\}$.
3.  **Compare the informational content.**
    *   Each outcome of $S_2$ corresponds to a compound event in $S_1$: $\text{even} = \{2, 4, 6\}$ and $\text{odd} = \{1, 3, 5\}$.
    *   Knowing the outcome in $S_1$ uniquely determines the outcome in $S_2$, but knowing the outcome in $S_2$ does not recover the exact face value; hence, $S_1$ provides strictly more information.
    *   **Answer: Comparison** — $S_1$ is a finer partition than $S_2$.

**Answer:** $S_1 = \{1, 2, 3, 4, 5, 6\}$ and $S_2 = \{\text{even}, \text{odd}\}$, where $S_1$ provides more information than $S_2$.
</details>

##### **4.3. Component Lifetime Event** (Lecture, Example 3)

For a component whose lifetime in years has sample space S = {t | t >= 0}, describe the subset corresponding to the event that it fails before the end of the fifth year.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** An event in a continuous sample space is defined by selecting the subset of real numbers satisfying the given physical boundary condition.

1.  **State the universal sample space.**
    *   Lifetime cannot be negative: $S = \{t \in \mathbb{R} \mid t \ge 0\} = [0, \infty)$.
    *   **Answer: Sample space** — $[0, \infty)$.
2.  **Formulate the failure event as a condition.**
    *   Failing before the end of year 5 means the elapsed time $t$ satisfies $t < 5$.
    *   Intersecting with $S$ yields $0 \le t < 5$.
    *   **Answer: Subset formulation** — $A = \{t \mid 0 \le t < 5\} = [0, 5)$.

**Answer:** $A = \{t \mid 0 \le t < 5\} = [0, 5)$
</details>

##### **4.4. Intersection of Student Attributes** (Lecture, Example 4)

Let E be the event that a randomly chosen student majors in engineering and F be the event that the student is female. Describe the event E ∩ F.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** The set intersection $E \cap F$ corresponds to the logical conjunction of the defining conditions of both events occurring simultaneously.

1.  **Interpret the components.**
    *   Event $E$: the chosen student is an engineering major.
    *   Event $F$: the chosen student is female.
    *   **Answer: Components** — $E = \text{engineering}$, $F = \text{female}$.
2.  **Apply the definition of intersection.**
    *   $E \cap F = \{\omega \in S \mid \omega \in E \text{ and } \omega \in F\}$.
    *   The outcome must satisfy both properties simultaneously.
    *   **Answer: Event description** — The set of all female engineering students in the classroom.
**Answer:** $E \cap F$ is the event that the chosen student is a female engineering student.
</details>

##### **4.5. Desk Items Complement** (Lecture, Example 5)

Given the sample space S = {book, cell phone, mp3, paper, stationery, laptop} and event A = {book, stationery, laptop, paper}, determine the complement A'.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** The complement $A'$ is the set difference $S \setminus A$, containing all sample points of $S$ that are not members of $A$.

1.  **List elements of the sample space and target set.**
    *   $S = \{\text{book}, \text{cell phone}, \text{mp3}, \text{paper}, \text{stationery}, \text{laptop}\}$.
    *   $A = \{\text{book}, \text{stationery}, \text{laptop}, \text{paper}\}$.
    *   **Answer: Sets identified** — $|S| = 6, |A| = 4$.
2.  **Subtract elements of A from S.**
    *   Removing $\text{book}, \text{stationery}, \text{laptop}, \text{paper}$ leaves $\text{cell phone}$ and $\text{mp3}$.
    *   $A' = \{\text{cell phone}, \text{mp3}\}$.
    *   **Answer: Complement** — $\{\text{cell phone}, \text{mp3}\}$.

**Answer:** $A' = \{\text{cell phone}, \text{mp3}\}$
</details>

##### **4.6. Television Network Exclusivity** (Lecture, Example 6)

A cable package has eight channels across several networks, including ABC, NBC, and CBS. Determine whether selecting an NBC channel and selecting a CBS channel are mutually exclusive events.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Two events are mutually exclusive (disjoint) if and only if their intersection is the empty set ($A \cap B = \emptyset$).

1.  **Define the events.**
    *   Let $A$ be the event that the selected channel belongs to NBC.
    *   Let $B$ be the event that the selected channel belongs to CBS.
    *   **Answer: Events established** — $A = \text{NBC channels}, B = \text{CBS channels}$.
2.  **Determine the intersection.**
    *   Every channel on the network lineup is affiliated with exactly one distinct network; no channel is simultaneously NBC and CBS.
    *   Therefore, $A \cap B = \emptyset$.
    *   **Answer: Disjointness confirmed** — $A \cap B = \emptyset$.

**Answer:** Mutually exclusive, because no channel is affiliated with both NBC and CBS ($A \cap B = \emptyset$).
</details>

##### **4.7. Union of Letter Sets** (Lecture, Example 7)

Find the union of the sets A = {a, b, c} and B = {b, c, d, e}.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** The union $A \cup B$ includes every element belonging to $A$, to $B$, or to both, with duplicate elements listed once.

1.  **Aggregate elements from both sets.**
    *   $A = \{a, b, c\}$.
    *   $B = \{b, c, d, e\}$.
    *   **Answer: Source elements** — $a, b, c$ and $b, c, d, e$.
2.  **Combine and remove duplicates.**
    *   $A \cup B = \{a, b, c, d, e\}$.
    *   **Answer: Union set** — $\{a, b, c, d, e\}$.

**Answer:** $A \cup B = \{a, b, c, d, e\}$
</details>

##### **4.8. Union of Habits and Intervals** (Lecture, Example 8)

Characterize the union of employee smoking and drinking events, and find the union of open intervals (3, 9) and (5, 12).

<details>
<summary>Click to see the solution</summary>

**Key Concept:** The set union represents the inclusive logical disjunction across both discrete qualitative events and continuous numerical intervals.

1.  **Characterize the employee event union.**
    *   Let $P$ be the event that an employee smokes cigarettes.
    *   Let $Q$ be the event that an employee drinks alcoholic beverages.
    *   $P \cup Q$ consists of all employees who smoke cigarettes, drink alcohol, or engage in both habits.
    *   **Answer: Qualitative union** — The set of employees who smoke, drink, or do both.
2.  **Compute the union of the open intervals.**
    *   Let $M = (3, 9) = \{x \in \mathbb{R} \mid 3 < x < 9\}$.
    *   Let $N = (5, 12) = \{y \in \mathbb{R} \mid 5 < y < 12\}$.
    *   Since $5 < 9$, the intervals overlap over $(5, 9)$. The combined range spans the lowest left bound to the highest right bound:
        $$M \cup N = (3, 12) = \{z \in \mathbb{R} \mid 3 < z < 12\}.$$
    *   **Answer: Interval union** — $(3, 12)$.

**Answer:** $P \cup Q$ is the set of employees who smoke, drink, or do both; $(3, 9) \cup (5, 12) = (3, 12)$.
</details>

##### **4.9. At Least One Head in Two Tosses** (Lecture, Example 9)

A balanced coin is tossed twice. What is the probability that at least one head occurs?

<details>
<summary>Click to see the solution</summary>

**Key Concept:** For an equiprobable sample space, the probability of an event equals the number of favorable outcomes divided by the total number of outcomes.

1.  **Construct the sample space.**
    *   Tossing a coin twice gives $2^2 = 4$ equally likely outcomes:
        $$S = \{HH, HT, TH, TT\}.$$
    *   Each outcome has weight $w = 1/4$.
    *   **Answer: Sample space size** — $|S| = 4$.
2.  **Identify the favorable event.**
    *   "At least one head" includes all outcomes except $TT$:
        $$A = \{HH, HT, TH\}.$$
    *   **Answer: Event size** — $|A| = 3$.
3.  **Compute the probability.**
    *   $P(A) = \frac{|A|}{|S|} = \frac{3}{4} = 0.75$.
    *   **Answer: Probability** — $3/4$.

**Answer:** $P(A) = \frac{3}{4} = 0.75$
</details>

##### **4.10. Loaded Die Probability** (Lecture, Example 10)

A die is weighted so that an even number is twice as likely to occur as an odd number. Find the probability of rolling a number strictly less than 4.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** The probability of any event in a discrete space is the sum of the probabilities of its elementary outcomes, normalized such that $P(S) = 1$.

1.  **Set up the probability weights.**
    *   Let the probability of each odd number $\{1, 3, 5\}$ be $w$.
    *   Let the probability of each even number $\{2, 4, 6\}$ be $2w$.
    *   **Answer: Weights assignment** — $P(\text{odd}) = w, P(\text{even}) = 2w$.
2.  **Solve for the normalizing constant w.**
    *   The sum over all six elementary outcomes must equal 1:
        $$P(S) = w + 2w + w + 2w + w + 2w = 9w = 1 \implies w = \frac{1}{9}.$$
    *   Thus, $P(\{1\}) = P(\{3\}) = P(\{5\}) = 1/9$ and $P(\{2\}) = P(\{4\}) = P(\{6\}) = 2/9$.
    *   **Answer: Normalizing constant** — $w = 1/9$.
3.  **Calculate the probability of rolling less than 4.**
    *   The event is $E = \{1, 2, 3\}$.
    *   $P(E) = P(\{1\}) + P(\{2\}) + P(\{3\}) = \frac{1}{9} + \frac{2}{9} + \frac{1}{9} = \frac{4}{9}$.
    *   **Answer: Probability of E** — $4/9$.

**Answer:** $P(E) = \frac{4}{9}$
</details>

##### **4.11. Union and Intersection on Loaded Die** (Lecture, Example 11)

On the weighted die where even outcomes are twice as likely as odd ones, let A be rolling an even number and B be rolling a multiple of 3. Find P(A ∪ B) and P(A ∩ B).

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Compound events on weighted spaces are computed by summing the individual probabilities of their constituent sample points.

1.  **Identify weights and event subsets.**
    *   Weights: $P(\{k\}) = 1/9$ for $k \in \{1, 3, 5\}$; $P(\{k\}) = 2/9$ for $k \in \{2, 4, 6\}$.
    *   Even outcomes: $A = \{2, 4, 6\}$.
    *   Multiples of 3: $B = \{3, 6\}$.
    *   **Answer: Subsets** — $A = \{2, 4, 6\}, B = \{3, 6\}$.
2.  **Determine and evaluate the intersection.**
    *   $A \cap B = \{6\}$.
    *   $P(A \cap B) = P(\{6\}) = \frac{2}{9}$.
    *   **Answer: Intersection probability** — $2/9$.
3.  **Determine and evaluate the union.**
    *   $A \cup B = \{2, 3, 4, 6\}$.
    *   $P(A \cup B) = P(\{2\}) + P(\{3\}) + P(\{4\}) + P(\{6\}) = \frac{2}{9} + \frac{1}{9} + \frac{2}{9} + \frac{2}{9} = \frac{7}{9}$.
    *   **Answer: Union probability** — $7/9$.

**Answer:** $P(A \cup B) = \frac{7}{9}$ and $P(A \cap B) = \frac{2}{9}$
</details>

##### **4.12. Engineering Majors Selection** (Lecture, Example 12)

A class has 25 industrial, 10 mechanical, 10 electrical, and 8 civil engineering students. Find the probability that a randomly chosen student is (a) an industrial engineering major and (b) a civil or electrical engineering major.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** In a finite population with uniform selection probability, $P(E) = |E| / |S|$.

1.  **Calculate the total size of the sample space.**
    *   $|S| = 25 + 10 + 10 + 8 = 53$.
    *   **Answer: Total students** — 53.
2.  **Find the probability of selecting an industrial engineer.**
    *   Number of industrial engineering majors: $|I| = 25$.

1.  **Identify given event probabilities.**
    *   $P(A) = 0.8$.
    *   $P(B) = 0.6$.
    *   $P(I) = \frac{25}{53}$.
    *   **Answer: Part (a)** — $25/53$.
3.  **Find the probability of selecting a civil or electrical engineer.**
    *   Civil and electrical departments are disjoint: $|C \cup E| = |C| + |E| = 8 + 10 = 18$.
    *   $P(C \cup E) = \frac{18}{53}$.
    *   **Answer: Part (b)** — $18/53$.

**Answer:** (a) $P(I) = \frac{25}{53}$; (b) $P(C \cup E) = \frac{18}{53}$
</details>

##### **4.13. Job Offer Probabilities** (Lecture, Example 13)

A graduating student has probability 0.8 of an offer from company A, 0.6 from company B, and 0.5 from both. What is the probability of receiving at least one offer?

<details>
<summary>Click to see the solution</summary>

**Key Concept:** By the principle of inclusion-exclusion for two events, $P(A \cup B) = P(A) + P(B) - P(A \cap B)$.

1.  **Identify given event probabilities.**
    *   $P(A) = 0.8$.
    *   $P(B) = 0.6$.
    *   $P(A \cap B) = 0.5$.
    *   **Answer: Probabilities extracted** — $P(A) = 0.8, P(B) = 0.6, P(A \cap B) = 0.5$.
2.  **Apply the addition rule.**
    *   $P(A \cup B) = P(A) + P(B) - P(A \cap B) = 0.8 + 0.6 - 0.5 = 0.9$.
    *   **Answer: Union probability** — $0.9$.

**Answer:** $P(A \cup B) = 0.9$
</details>

##### **4.14. Sum of Seven or Eleven** (Lecture, Example 14)

What is the probability of rolling a total sum of 7 or 11 when a pair of fair dice is tossed?

<details>
<summary>Click to see the solution</summary>

**Key Concept:** When two events cannot occur simultaneously, their union probability equals the sum of their individual probabilities: $P(A \cup B) = P(A) + P(B)$.

1.  **Determine the total number of outcomes.**
    *   Tossing two fair dice yields $|S| = 6 \times 6 = 36$ equiprobable pairs.
    *   **Answer: Total outcomes** — 36.
2.  **Enumerate favorable outcomes for sums of 7 and 11.**
    *   Sum is 7 ($A$): $\{(1, 6), (2, 5), (3, 4), (4, 3), (5, 2), (6, 1)\} \implies |A| = 6$.
    *   Sum is 11 ($B$): $\{(5, 6), (6, 5)\} \implies |B| = 2$.
    *   **Answer: Favorable counts** — $|A| = 6, |B| = 2$.
3.  **Compute individual and union probabilities.**
    *   Since a roll cannot sum to both 7 and 11, $A \cap B = \emptyset$.
    *   $P(A \cup B) = P(A) + P(B) = \frac{6}{36} + \frac{2}{36} = \frac{8}{36} = \frac{2}{9}$.
    *   **Answer: Final probability** — $2/9$.

**Answer:** $P(A \cup B) = \frac{2}{9}$
</details>

##### **4.15. Automobile Servicing Complement** (Lecture, Example 15)

A mechanic services 3, 4, 5, 6, 7, or 8+ cars with probabilities 0.12, 0.19, 0.28, 0.24, 0.10, and 0.07 respectively. What is the probability that he services at least 5 cars on a workday?

<details>
<summary>Click to see the solution</summary>

**Key Concept:** The probability of an event can be efficiently computed via its complement: $P(E) = 1 - P(E^c)$.

1.  **Define the target event and its complement.**
    *   Let $E$ be the event of servicing $\ge 5$ cars: $E = \{5, 6, 7, 8+\}$.
    *   The complement $E^c$ represents servicing strictly fewer than 5 cars: $E^c = \{3, 4\}$.
    *   **Answer: Complement identified** — $E^c = \{3, 4\}$.
2.  **Calculate the complement probability.**
    *   $P(E^c) = P(3) + P(4) = 0.12 + 0.19 = 0.31$.
    *   **Answer: Complement probability** — $0.31$.
3.  **Subtract from unity.**
    *   $P(E) = 1 - P(E^c) = 1 - 0.31 = 0.69$.
    *   **Answer: Target probability** — $0.69$.

**Answer:** $P(E) = 0.69$
</details>

##### **4.16. Single Die Roll Four** (Lecture, Example 16)

What is the probability of rolling a 4 on a fair six-sided die?

<details>
<summary>Click to see the solution</summary>

**Key Concept:** In a classical probability model with $n$ equally likely elementary outcomes, $P(A) = m / n$.

1.  **Identify total and favorable outcomes.**
    *   Sample space: $S = \{1, 2, 3, 4, 5, 6\}$, so $n = 6$.
    *   Event: $A = \{4\}$, so $m = 1$.
    *   **Answer: Outcome counts** — $m = 1, n = 6$.
2.  **Calculate the probability.**
    *   $P(A) = \frac{m}{n} = \frac{1}{6}$.
    *   **Answer: Probability** — $1/6$.

**Answer:** $P(\{4\}) = \frac{1}{6}$
</details>

##### **4.17. Meeting Probability Within Fifteen Minutes** (Lecture, Example 17)

Two friends arrive independently and uniformly at random at a meeting spot between 12:00 and 13:00, each waiting up to 15 minutes. What is the probability that they meet?

<details>
<summary>Click to see the solution</summary>

**Key Concept:** In geometric probability over continuous 2D domains, probability equals the ratio of the favorable area to the total sample space area.

1.  **Model the sample space.**
    *   Let $x \in [0, 60]$ be Alice's arrival time in minutes after 12:00.
    *   Let $y \in [0, 60]$ be Bob's arrival time in minutes after 12:00.
    *   Total area is $\text{Area}(S) = 60 \times 60 = 3600$.
    *   **Answer: Total area** — 3600.
2.  **Formulate the meeting condition.**
    *   They meet if and only if $|x - y| \le 15$, defining the diagonal strip $x - 15 \le y \le x + 15$.
    *   **Answer: Favorable region** — Strip $|x - y| \le 15$.
3.  **Compute the non-meeting area.**
    *   The complement region consists of two right triangles:
        *   Upper-left: vertices $(0, 15), (0, 60), (45, 60)$, with legs $60 - 15 = 45$.
        *   Lower-right: vertices $(15, 0), (60, 0), (60, 45)$, with legs 45.
    *   $\text{Area}(\text{Non-Meeting}) = 2 \times \left(\frac{1}{2} \times 45 \times 45\right) = 45^2 = 2025$.
    *   **Answer: Non-meeting area** — 2025.
4.  **Evaluate the probability.**
    *   $\text{Area}(\text{Meeting}) = 3600 - 2025 = 1575$.
    *   $P(\text{Meeting}) = \frac{1575}{3600} = \frac{7}{16} = 0.4375$.
    *   **Answer: Meeting probability** — $7/16$.

**Answer:** $P(\text{Meeting}) = \frac{7}{16} = 0.4375$
</details>

##### **4.18. Three Coin Tosses** (Tutorial, Task 1)

For three tosses of a fair coin, specify the sample space, express events for exactly two heads, at least one head, and first toss head, compute their probabilities, and verify the addition rule.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Equiprobable discrete sample spaces allow exact calculation of subset probabilities and empirical verification of the inclusion-exclusion principle.

1.  **Specify the sample space.**
    *   Each toss has 2 outcomes, giving $2^3 = 8$ outcomes:
        $$S = \{HHH, HHT, HTH, THH, HTT, THT, TTH, TTT\}, \quad |S| = 8.$$
    *   **Answer: Sample space size** — $|S| = 8$.
2.  **Define events as explicit subsets.**
    *   $A$ (exactly two heads): $A = \{HHT, HTH, THH\}$, $|A| = 3$.
    *   $B$ (at least one head): $B = S \setminus \{TTT\}$, $|B| = 7$.
    *   $C$ (first toss is a head): $C = \{HHH, HHT, HTH, HTT\}$, $|C| = 4$.
    *   **Answer: Event subsets** — $|A| = 3, |B| = 7, |C| = 4$.
3.  **Calculate individual probabilities and interactions.**
    *   $P(A) = \frac{3}{8}$, $P(B) = \frac{7}{8}$, $P(C) = \frac{4}{8} = \frac{1}{2}$.
    *   $A \cap C = \{HHT, HTH\} \implies P(A \cap C) = \frac{2}{8} = \frac{1}{4}$.
    *   $A \cup C = \{HHH, HHT, HTH, HTT, THH\} \implies P(A \cup C) = \frac{5}{8}$.
    *   **Answer: Probabilities** — $P(A) = 3/8, P(B) = 7/8, P(C) = 1/2, P(A \cap C) = 1/4, P(A \cup C) = 5/8$.
4.  **Verify the addition rule.**
    *   $P(A) + P(C) - P(A \cap C) = \frac{3}{8} + \frac{4}{8} - \frac{2}{8} = \frac{5}{8}$.
    *   This equals $P(A \cup C) = \frac{5}{8}$.
    *   **Answer: Verification** — Identity holds.

**Answer:** $P(A) = \frac{3}{8}, P(B) = \frac{7}{8}, P(C) = \frac{1}{2}, P(A \cap C) = \frac{1}{4}, P(A \cup C) = \frac{5}{8}$, satisfying $P(A \cup C) = P(A) + P(C) - P(A \cap C)$.
</details>

##### **4.19. Server Cluster Operations and De Morgan Law** (Tutorial, Task 2)

In a three-server cluster, express in set operations the events that all servers are up, at least one is down, exactly one is up, and at least two are up, and prove De Morgan's law.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Complex operational states in distributed systems translate to boolean algebra on events, with De Morgan's law formalizing the duality between conjunction and disjunction.

1.  **Express the operational events.**
    *   (1) All three up: $D_1 = A_1 \cap A_2 \cap A_3$.
    *   (2) At least one down: negation of all up, $D_2 = (A_1 \cap A_2 \cap A_3)^c = A_1^c \cup A_2^c \cup A_3^c$.
    *   (3) Exactly one up: partition by which server is operational:
        $$D_3 = (A_1 \cap A_2^c \cap A_3^c) \cup (A_1^c \cap A_2 \cap A_3^c) \cup (A_1^c \cap A_2^c \cap A_3).$$
    *   (4) At least two up: any pair active (covering all three):
        $$D_4 = (A_1 \cap A_2) \cup (A_1 \cap A_3) \cup (A_2 \cap A_3).$$
    *   **Answer: Event expressions** — Defined as above.
2.  **Prove De Morgan's law elementwise.**
    *   Let $\omega \in S$:
        $$\omega \in (A_1 \cap A_2 \cap A_3)^c \iff \omega \notin (A_1 \cap A_2 \cap A_3)$$
        $$\iff \neg(\omega \in A_1 \land \omega \in A_2 \land \omega \in A_3)$$
        $$\iff (\omega \notin A_1) \lor (\omega \notin A_2) \lor (\omega \notin A_3)$$
        $$\iff \omega \in A_1^c \lor \omega \in A_2^c \lor \omega \in A_3^c$$
        $$\iff \omega \in (A_1^c \cup A_2^c \cup A_3^c).$$
    *   Both directions hold, proving set equality.
    *   **Answer: Proof completed** — Mutual containment established.

**Answer:** $D_1 = A_1 \cap A_2 \cap A_3$, $D_2 = A_1^c \cup A_2^c \cup A_3^c$, $D_3 = \bigcup_{\text{cyclic}} (A_i \cap A_j^c \cap A_k^c)$, $D_4 = (A_1 \cap A_2) \cup (A_1 \cap A_3) \cup (A_2 \cap A_3)$, with De Morgan's identity proven.
</details>

##### **4.20. Exam Passing Probabilities** (Tutorial, Task 3)

Given passing probabilities of 0.7 for Calculus, 0.6 for Linear Algebra, and 0.5 for both, find the probability of passing at least one, neither, exactly one, and Calculus but not Linear Algebra.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Decomposing overlapping events into disjoint components enables straightforward computation of arbitrary Boolean combinations.

1.  **Passes at least one exam.**
    *   $P(M \cup L) = P(M) + P(L) - P(M \cap L) = 0.7 + 0.6 - 0.5 = 0.8$.
    *   **Answer: At least one** — $0.8$.
2.  **Passes neither exam.**
    *   By De Morgan's law, $M^c \cap L^c = (M \cup L)^c$.
    *   $P(M^c \cap L^c) = 1 - P(M \cup L) = 1 - 0.8 = 0.2$.
    *   **Answer: Neither** — $0.2$.
3.  **Passes exactly one exam.**
    *   Subtract the intersection from the union:
        $$P((M \cup L) \setminus (M \cap L)) = P(M \cup L) - P(M \cap L) = 0.8 - 0.5 = 0.3.$$
    *   **Answer: Exactly one** — $0.3$.
4.  **Passes Calculus but fails Linear Algebra.**
    *   Decompose $M$ as $(M \cap L) \sqcup (M \setminus L)$:
        $$P(M \setminus L) = P(M) - P(M \cap L) = 0.7 - 0.5 = 0.2$.
    *   **Answer: Calculus only** — $0.2$.

**Answer:** (1) 0.8, (2) 0.2, (3) 0.3, (4) 0.2
</details>

##### **4.21. Inclusion-Exclusion for Three Events** (Tutorial, Task 4)

Given probabilities of individual events, pairwise intersections, and the triple intersection for three events, compute the probability that at least one, none, at least two, and exactly one occur.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** The three-event inclusion-exclusion formula accounts for multiple counting across pairwise and triple intersections.

1.  **Compute probability of at least one event.**
    *   $$P(A \cup B \cup C) = \sum P(A) - \sum_{i < j} P(A_i \cap A_j) + P(A \cap B \cap C)$$
        $$= (0.5 + 0.5 + 0.5) - (0.3 + 0.3 + 0.2) + 0.1 = 1.5 - 0.8 + 0.1 = 0.8.$$
    *   **Answer: At least one** — $0.8$.
2.  **Compute probability of none occurring.**
    *   $P(A^c \cap B^c \cap C^c) = 1 - P(A \cup B \cup C) = 1 - 0.8 = 0.2$.
    *   **Answer: None** — $0.2$.
3.  **Compute probability that at least two occur.**
    *   The pairwise sum counts elements in exactly two events once, and in all three events three times. Subtracting $2 P(A \cap B \cap C)$ leaves each counted once:
        $$P(\text{at least 2}) = \sum_{i < j} P(A_i \cap A_j) - 2P(A \cap B \cap C) = 0.8 - 2(0.1) = 0.6.$$
    *   **Answer: At least two** — $0.6$.
4.  **Compute probability that exactly one occurs.**
    *   Subtract outcomes belonging to at least two events from the total union:
        $$P(\text{exactly 1}) = P(A \cup B \cup C) - P(\text{at least 2}) = 0.8 - 0.6 = 0.2.$$
    *   **Answer: Exactly one** — $0.2$.

**Answer:** (1) 0.8, (2) 0.2, (3) 0.6, (4) 0.2
</details>

##### **4.22. Set Operations Probability Computations** (Tutorial, Task 5)

Given P(A) = 0.4, P(B) = 0.5, and P(A ∪ B) = 0.7, calculate P(A ∩ B), P(A^c ∩ B^c), P(A \ B), P(A^c ∪ B^c), and P(A Δ B).

<details>
<summary>Click to see the solution</summary>

**Key Concept:** All Boolean set probabilities follow directly from the addition theorem, the complement rule, and De Morgan's laws.

1.  **Find the intersection probability.**
    *   $P(A \cap B) = P(A) + P(B) - P(A \cup B) = 0.4 + 0.5 - 0.7 = 0.2$.
    *   **Answer: $P(A \cap B)$** — $0.2$.
2.  **Find the joint complement probability.**
    *   $P(A^c \cap B^c) = P((A \cup B)^c) = 1 - P(A \cup B) = 1 - 0.7 = 0.3$.
    *   **Answer: $P(A^c \cap B^c)$** — $0.3$.
3.  **Find the set difference probability.**
    *   $P(A \setminus B) = P(A) - P(A \cap B) = 0.4 - 0.2 = 0.2$.
    *   **Answer: $P(A \setminus B)$** — $0.2$.
4.  **Find the union of complements.**
    *   $P(A^c \cup B^c) = P((A \cap B)^c) = 1 - P(A \cap B) = 1 - 0.2 = 0.8$.
    *   **Answer: $P(A^c \cup B^c)$** — $0.8$.
5.  **Find the symmetric difference probability.**
    *   $P(A \Delta B) = P(A \cup B) - P(A \cap B) = 0.7 - 0.2 = 0.5$.
    *   **Answer: $P(A \Delta B)$** — $0.5$.

**Answer:** $P(A \cap B) = 0.2, P(A^c \cap B^c) = 0.3, P(A \setminus B) = 0.2, P(A^c \cup B^c) = 0.8, P(A \Delta B) = 0.5$
</details>

##### **4.23. Defective Items Sampling Without Replacement** (Tutorial, Task 6)

Four items are drawn without replacement from a batch of 10 items containing 3 defectives. Find the probabilities of getting zero, exactly one, and at least one defective item.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Combinations determine probabilities in sampling without replacement via the hypergeometric model.

1.  **Compute total combinations of drawing 4 items.**
    *   $n = \binom{10}{4} = \frac{10 \times 9 \times 8 \times 7}{4 \times 3 \times 2 \times 1} = 210$.
    *   **Answer: Total combinations** — 210.
2.  **Compute probability of zero defectives.**
    *   All 4 items must be chosen from the 7 non-defective items:
        $$P(A_0) = \frac{\binom{7}{4}\binom{3}{0}}{\binom{10}{4}} = \frac{35}{210} = \frac{1}{6} \approx 0.167.$$
    *   **Answer: Zero defectives** — $1/6$.
3.  **Compute probability of exactly one defective.**
    *   Choose 1 defective from 3 and 3 good from 7:
        $$P(A_1) = \frac{\binom{3}{1}\binom{7}{3}}{\binom{10}{4}} = \frac{3 \times 35}{210} = \frac{105}{210} = \frac{1}{2} = 0.5.$$
    *   **Answer: Exactly one defective** — $1/2$.
4.  **Compute probability of at least one defective.**
    *   Use the complement of zero defectives:
        $$P(\text{at least 1}) = 1 - P(A_0) = 1 - \frac{1}{6} = \frac{5}{6} \approx 0.833.$$
    *   **Answer: At least one defective** — $5/6$.

**Answer:** $P(\text{none}) = \frac{1}{6}$, $P(\text{one}) = \frac{1}{2}$, $P(\text{at least one}) = \frac{5}{6}$
</details>

##### **4.24. Meeting Time and Waiting Duration** (Tutorial, Task 7)

Two people arrive uniformly between 12:00 and 13:00 waiting 15 minutes; find the probability they meet, and determine the required waiting time for the meeting probability to be at least 1/2.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Parameterizing the geometric probability strip allows solving inverse boundary problems for continuous uniform variables.

1.  **Derive the meeting probability as a function of waiting time t.**
    *   For waiting time $t \in [0, 60]$, the non-meeting region consists of two right triangles with leg lengths $60 - t$.
    *   $$P(A_t) = 1 - \frac{2 \times \frac{1}{2}(60 - t)^2}{3600} = 1 - \left(\frac{60 - t}{60}\right)^2.$$
    *   **Answer: General formula** — $P(A_t) = 1 - ((60 - t)/60)^2$.
2.  **Evaluate for t = 15 minutes.**
    *   $$P(A_{15}) = 1 - \left(\frac{45}{60}\right)^2 = 1 - \left(\frac{3}{4}\right)^2 = 1 - \frac{9}{16} = \frac{7}{16} = 0.4375.$$
    *   **Answer: Part 1** — $7/16$.
3.  **Solve for meeting probability at least 1/2.**
    *   Set $P(A_t) \ge \frac{1}{2}$:
        $$1 - \left(\frac{60 - t}{60}\right)^2 \ge \frac{1}{2} \iff \left(\frac{60 - t}{60}\right)^2 \le \frac{1}{2}.$$
    *   Take the positive square root:
        $$\frac{60 - t}{60} \le \frac{1}{\sqrt{2}} \iff 60 - t \le \frac{60}{\sqrt{2}} = 30\sqrt{2}.$$
    *   Isolate $t$:
        $$t \ge 60 - 30\sqrt{2} \approx 60 - 42.426 = 17.57 \text{ minutes} \approx 17 \text{ min } 35 \text{ sec}.$$
    *   **Answer: Part 2** — $t \ge 60 - 30\sqrt{2} \approx 17.57 \text{ minutes}$.

**Answer:** $P(\text{meeting}) = \frac{7}{16} \approx 0.4375$; waiting time must be at least $60 - 30\sqrt{2} \approx 17.57\text{ minutes}$.
</details>

##### **4.25. Broken Stick Triangle Condition** (Tutorial, Task 8)

A stick of unit length is broken at two uniformly and independently chosen points. Find the probability that the three resulting segments can form a triangle.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Three pieces of total length 1 can form a triangle if and only if every individual piece has length strictly less than $1/2$.

1.  **Define the sample space.**
    *   Let $x, y \in [0, 1]$ be the two break positions, so $S = [0, 1]^2$ with $\text{Area}(S) = 1$.
    *   **Answer: Domain** — Unit square of area 1.
2.  **Analyze the case x < y.**
    *   The segment lengths are $L_1 = x$, $L_2 = y - x$, and $L_3 = 1 - y$.
    *   The triangle inequality requires:
        $$x < \frac{1}{2}, \quad y - x < \frac{1}{2}, \quad 1 - y < \frac{1}{2} \iff y > \frac{1}{2}.$$
    *   This defines a triangle with vertices $(0, 1/2)$, $(1/2, 1/2)$, and $(1/2, 1)$.
    *   $\text{Area} = \frac{1}{2} \times \text{base} \times \text{height} = \frac{1}{2} \times \frac{1}{2} \times \frac{1}{2} = \frac{1}{8}$.
    *   **Answer: Area for $x < y$** — $1/8$.
3.  **Analyze the case x > y.**
    *   By symmetry across the line $y = x$, the triangle inequality requires $y < 1/2$, $x - y < 1/2$, and $x > 1/2$.
    *   This forms an identical triangle of area $1/8$.
    *   **Answer: Area for $x > y$** — $1/8$.
4.  **Sum both favorable regions.**
    *   $P = \frac{1}{8} + \frac{1}{8} = \frac{1}{4} = 0.25$.
    *   **Answer: Total probability** — $1/4$.

**Answer:** $P = \frac{1}{4} = 0.25$
</details>
##### **4.26. Axiom Verification on Finite Space** (Tutorial, Task 9)

Determine which of four specified set functions on a four-element sample space is a valid probability measure, identifying the failing axiom for invalid candidates and computing probabilities for the valid one.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** A valid probability measure on a finite set must satisfy non-negativity ($P(A) \ge 0$), normalization ($P(S) = 1$), and finite additivity on disjoint sets.

1.  **Test candidate (a).**
    *   $\sum_{k=1}^4 P(\{k\}) = 0.3 + 0.3 + 0.3 + 0.2 = 1.1 \ne 1$.
    *   Fails the normalization axiom.
    *   **Answer: Candidate (a)** — Invalid (fails normalization).
2.  **Test candidate (b).**
    *   $P(\{4\}) = -0.2 < 0$.
    *   Fails the non-negativity axiom.
    *   **Answer: Candidate (b)** — Invalid (fails non-negativity).
3.  **Test candidate (c).**
    *   $P(A) = (|A|/4)^2$. Non-negativity holds and $P(S) = (4/4)^2 = 1$.
    *   Check additivity on disjoint singletons $\{1\}$ and $\{2\}$:
        $$P(\{1\}) + P(\{2\}) = \frac{1}{16} + \frac{1}{16} = \frac{1}{8}, \quad P(\{1, 2\}) = \left(\frac{2}{4}\right)^2 = \frac{4}{16} = \frac{1}{4}.$$
    *   Since $1/8 \ne 1/4$, additivity fails.
    *   **Answer: Candidate (c)** — Invalid (fails additivity).
4.  **Test candidate (d) and compute requested probabilities.**
    *   $P(A) = \frac{1}{10}\sum_{k \in A} k$.
    *   All terms are positive; $P(S) = \frac{1+2+3+4}{10} = \frac{10}{10} = 1$; summation over disjoint sets is strictly additive. Valid measure.
    *   $P(\{1, 3\}) = \frac{1 + 3}{10} = \frac{4}{10} = 0.4$.
    *   $P(\{2, 4\}) = \frac{2 + 4}{10} = \frac{6}{10} = 0.6$.
    *   **Answer: Candidate (d)** — Valid; $P(\{1, 3\}) = 0.4, P(\{2, 4\}) = 0.6$.

**Answer:** Only (d) is a valid probability measure ((a) violates normalization, (b) violates non-negativity, (c) violates additivity); $P(\{1, 3\}) = 0.4$ and $P(\{2, 4\}) = 0.6$.
</details>

##### **4.27. Geometric Weights and Finite-Infinite Measures** (Tutorial, Task 10)

Determine the normalizing constant c for P({k}) = c/3^k on natural numbers, find probabilities of even outcomes and k >= 3, and test if indicator Q(A) of set infinitude is a probability measure.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Countable additivity on infinite sample spaces requires convergence of infinite series to 1, while global property indicators fail finite additivity.

1.  **Solve for normalizing constant c in part (a).**
    *   Normalization requires $\sum_{k=1}^\infty P(\{k\}) = 1$:
        $$\sum_{k=1}^\infty \frac{c}{3^k} = c \frac{1/3}{1 - 1/3} = c \frac{1/3}{2/3} = \frac{c}{2} = 1 \implies c = 2.$$
    *   **Answer: Normalizing constant** — $c = 2$.
2.  **Compute the probability of an even number in part (a).**
    *   Even values correspond to $k = 2m$ for $m \ge 1$:
        $$P(\text{even}) = \sum_{m=1}^\infty \frac{2}{3^{2m}} = \sum_{m=1}^\infty \frac{2}{9^m} = 2 \frac{1/9}{1 - 1/9} = 2 \frac{1/9}{8/9} = 2 \times \frac{1}{8} = \frac{1}{4}.$$
    *   **Answer: Even outcomes** — $1/4$.
3.  **Compute the probability of k >= 3 in part (a).**
    *   Using the complement:
        $$P(k \ge 3) = 1 - P(\{1\}) - P(\{2\}) = 1 - \frac{2}{3} - \frac{2}{9} = \frac{9 - 6 - 2}{9} = \frac{1}{9}.$$
    *   **Answer: Tail probability** — $1/9$.
4.  **Evaluate set function Q in part (b).**
    *   $Q(A) = 0$ for finite $A$, $Q(A) = 1$ for infinite $A$.
    *   Non-negativity holds ($Q(A) \in \{0, 1\}$) and $Q(S) = 1$ because $S = \mathbb{N}$ is infinite.
    *   Test additivity on disjoint sets $A = \{1, 3, 5, \dots\}$ and $B = \{2, 4, 6, \dots\}$:
        $$Q(A) + Q(B) = 1 + 1 = 2 \ne Q(A \cup B) = Q(S) = 1.$$
    *   Finite additivity is violated; moreover, $\sum_{k=1}^\infty Q(\{k\}) = \sum 0 = 0 \ne Q(S) = 1$ violates countable additivity.
    *   **Answer: Q validity** — $Q$ is not a probability measure.

**Answer:** (a) $c = 2$, $P(\text{even}) = \frac{1}{4}$, $P(k \ge 3) = \frac{1}{9}$; (b) $Q$ is not a probability measure because additivity fails.
</details>

##### **4.28. Sigma-Algebra Identification and Generation** (Tutorial, Task 11)

Check whether four candidate collections of subsets on a four-element set are sigma-algebras, and construct the minimal sigma-algebra generated by two singleton sets.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** A $\sigma$-algebra must contain the whole space and be closed under complementation and unions; on a finite space it is generated by taking all unions of partition atoms.

1.  **Check collection $\mathcal{F}_1 = \{\emptyset, \{a\}, \{b, c, d\}, X\}$.**
    *   $X \in \mathcal{F}_1$; complements: $\{a\}^c = \{b, c, d\} \in \mathcal{F}_1$; unions: $\{a\} \cup \{b, c, d\} = X \in \mathcal{F}_1$.
    *   Valid $\sigma$-algebra.
    *   **Answer: $\mathcal{F}_1$** — Is a $\sigma$-algebra.
2.  **Check collection $\mathcal{F}_2 = \{\emptyset, \{a\}, \{b\}, \{a, b\}, X\}$.**
    *   The complement $\{a\}^c = \{b, c, d\} \notin \mathcal{F}_2$.
    *   Fails closure under complements.
    *   **Answer: $\mathcal{F}_2$** — Not a $\sigma$-algebra.
3.  **Check collection $\mathcal{F}_3 = \{\emptyset, \{a, b\}, \{c, d\}, X\}$.**
    *   $X \in \mathcal{F}_3$; $\{a, b\}^c = \{c, d\} \in \mathcal{F}_3$; $\{a, b\} \cup \{c, d\} = X \in \mathcal{F}_3$.
    *   Valid $\sigma$-algebra.
    *   **Answer: $\mathcal{F}_3$** — Is a $\sigma$-algebra.
4.  **Check collection $\mathcal{F}_4 = \{\emptyset, \{a\}, \{b, c, d\}, \{a, b\}, \{c, d\}, X\}$.**
    *   All complements are present, but the union $\{a\} \cup \{c, d\} = \{a, c, d\} \notin \mathcal{F}_4$.
    *   Fails closure under unions.
    *   **Answer: $\mathcal{F}_4$** — Not a $\sigma$-algebra.
5.  **Construct the minimal $\sigma$-algebra $\sigma(\{a\}, \{b\})$.**
    *   Find the partition atoms: $\{a\}$, $\{b\}$, and $(\{a\} \cup \{b\})^c = \{c, d\}$.
    *   With $n = 3$ atoms, the minimal $\sigma$-algebra consists of all $2^3 = 8$ possible unions:
        $$\sigma(\{a\}, \{b\}) = \{\emptyset, \{a\}, \{b\}, \{c, d\}, \{a, b\}, \{a, c, d\}, \{b, c, d\}, X\}.$$
    *   **Answer: Generated $\sigma$-algebra** — 8-element collection.

**Answer:** $\mathcal{F}_1$ and $\mathcal{F}_3$ are $\sigma$-algebras; $\mathcal{F}_2$ and $\mathcal{F}_4$ are not; $\sigma(\{a\}, \{b\}) = \{\emptyset, \{a\}, \{b\}, \{c, d\}, \{a, b\}, \{a, c, d\}, \{b, c, d\}, X\}$.
</details>

##### **4.29. Axiomatic Probability and Sigma-Algebra Properties** (Tutorial, Task 12)

Prove P(∅) = 0 and monotonicity from Kolmogorov axioms, prove that the intersection of two sigma-algebras is a sigma-algebra, and provide a counterexample showing the union need not be.

<details>
<summary>Click to see the solution</summary>

**Key Concept:** Monotonicity and empty-set probability derive strictly from finite additivity, while $\sigma$-algebra structures are preserved under intersections but not under arbitrary unions.

1.  **Prove $P(\emptyset) = 0$ and monotonicity from axioms.**
    *   *Empty set:* The sets $S$ and $\emptyset$ are disjoint ($S \cap \emptyset = \emptyset$) and $S \cup \emptyset = S$. By additivity:
        $$P(S) = P(S \cup \emptyset) = P(S) + P(\emptyset).$$
        Subtracting $P(S) = 1$ from both sides yields $P(\emptyset) = 0$.
    *   *Monotonicity:* Suppose $A \subseteq B$. Decompose $B$ into disjoint sets: $B = A \sqcup (B \setminus A)$. By additivity:
        $$P(B) = P(A) + P(B \setminus A).$$
        By the non-negativity axiom, $P(B \setminus A) \ge 0$, which directly implies:
        $$P(B) \ge P(A).$$
    *   **Answer: Axiom deductions complete** — Monotonicity and $P(\emptyset) = 0$ proven.
2.  **Prove that the intersection $\mathcal{F}_1 \cap \mathcal{F}_2$ is a $\sigma$-algebra.**
    *   Let $\mathcal{F} = \mathcal{F}_1 \cap \mathcal{F}_2$.
    *   (i) Since $\mathcal{F}_1$ and $\mathcal{F}_2$ are $\sigma$-algebras on $X$, $X \in \mathcal{F}_1$ and $X \in \mathcal{F}_2$, so $X \in \mathcal{F}$.
    *   Notice that $\{1\} \in \mathcal{F}_1 \cup \mathcal{F}_2$ and $\{2\} \in \mathcal{F}_1 \cup \mathcal{F}_2$, but their union $\{1\} \cup \{2\} = \{1, 2\} \notin \mathcal{F}_1 \cup \mathcal{F}_2$.
    *   Hence, $\mathcal{F}_1 \cup \mathcal{F}_2$ fails closure under unions and is not a $\sigma$-algebra.
    *   **Answer: Counterexample verified** — Union fails closure.

**Answer:** $P(\emptyset) = 0$ and monotonicity follow from disjoint additivity and non-negativity; intersections of $\sigma$-algebras satisfy all axioms, while unions generally fail closure under unions (e.g., $\{1\} \cup \{2\} \notin \mathcal{F}_1 \cup \mathcal{F}_2$).
</details>
